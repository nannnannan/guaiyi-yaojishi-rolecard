# 《诡异药剂师》v0.7 真机验收步骤（待用户授权后执行）

> 本文件是计划文档。离线候选已构建通过（`npm run check`：21726 项检查），但**真机验收尚未执行**。以下任何真实宿主操作都必须先获得用户明确授权。

## 前置条件

- SillyTavern 1.17.0 + 酒馆助手（Tavern Helper）4.9.1 已运行
- 产物：`角色卡本体\诡异药剂师_MVU_v0.7\dist\诡异药剂师_v0.7.json`
- v0.7 离线基线：SHA-256 `c706b524238f6d7874babc914a1480f3036216ebe17b66dccf27b48a6ebeeea9`、2268983 字节

## 验收清单（每步都需要用户授权）

1. **导入**：导入 `dist/诡异药剂师_v0.7.json`，确认内嵌世界书 260 条目、data 版本 0.7.0、世界书 tavernweave 版本 0.7.0。
2. **替换/绑定世界书**：确认聊天绑定的是本卡内嵌世界书；若宿主已有 v0.6 世界书，按宿主流程替换，保留旧聊天只读。
3. **新建测试聊天**：全新聊天（不迁移 v0.6 旧聊天）。确认开场白：
   - E01 完成、E02 收尾活跃、E03 未触发；
   - 状态栏四页渲染正常（死界状态栏），FALLBACK_STATE 与 initvar 一致；
   - 推进按钮显示"推进：E03 …"（E02 收尾后）。
4. **首轮推进**：玩家写"继续营业"，确认原子迁移 E02→E03（`replace /事件`、预兆指向 E03、唯一活跃清空/正确）。**推进后按 F5 刷新页面并确认 E03 预兆仍在**——状态栏新版推进只通过 `Mvu.replaceMvuData` 写入、不再显式调用宿主 saveChat，持久化依赖宿主自动保存；若刷新后预兆丢失，需在 `src/ui/status.html` 的 `advanceCurrentEvent` 中补回显式 `saveChat` 调用（经用户授权后修改）。
4a. **备用开场白（E25 预兆）**：新建聊天时选择备用开场白，确认开场正文为幸福之家围猎暂歇场景；状态栏显示 E24 完成、E25 预兆（预兆指向 E25·双面伯爵与心脑之汤）、无活跃事件；爱丽丝隐去身形未消散、白逸与猪头屠夫在场；推进按钮按 E25 预兆状态显示（"已进入预兆：E25"）。
5. **阶段三门控抽查**（直接跳状态法不可用，按事件推进到相应锚点后观察）：
   - E36 前后：左左醉酒酒店事件蓝灯只在该窗口注入；黑弦月 E51 前不出现其人设内容；
   - E42：林樱登场锁释放（车祸初遇）；平行"林恩"身份疑云保持"不详"；
   - E48：喵喵登场、猫化诅咒、羽毛笔条目只输出已揭示内容；
   - E51：黑弦月到货、30 日契约；E52 森一四杀；
   - E54：艾雯爵士"疫医暗面"仅作推测输出；E60 圣徒叛变；
   - E63：堡垒解围；E64：只有阶段4待续钩子、无推进按钮、不出现 E65。
6. **关键词绿灯抽查**：聊天中提"黑弦月/喵喵/林樱/艾雯爵士/羽毛笔/a01/欲望母树"应激活对应人物条目；提概念名（如"死亡凋零"）应激活对应概念条目且同轮不重复注入。
7. **色色口径抽查**：女性角色亲密与恶堕无锁；"大鸡巴"直写；无幼态锁提示。
8. **状态栏推进边界**：E64 完成/变形后按钮显示"当前无可推进事件"；取消态前件显示"需正文判定局部依赖"。

## 验收通过后

- 更新 `host_acceptance.json`：status → `accepted-release`、记录真机 SHA-256、字节数与验收证据。
- 更新 AGENTS.md/README 的验收状态与基线（如产物未变则 SHA 不变）。
- 不通过：记录失败用例 → 修复源码 → 重新 `npm run check` → 重新真机验收。

## 红线

- 不删除/不覆盖用户的真实聊天与角色卡；一切操作先备份。
- 真机请求模型产生的聊天内容不得回写入源码。
- 未经用户授权不得对真实宿主做任何导入、替换、修改消息或清理对象操作。
