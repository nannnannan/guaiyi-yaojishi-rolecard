<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E466.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E467.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E468.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E472.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E473.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E491.状态", { defaults: "未触发" }))) { _%>
# C987·七神光宴·终端系

- 类别：物品／意识轻增量
- 事实适用：不扩写C987单文件百科；终端集群、信任度、金字塔等细节落在新条，本条只作轻量门控索引并引：C1361、C1363、C1364、C1367、C1368、C1385等。
- 定义：已确认光宴可以残片／终端宿主形态多实例存在，算力分流、替身入队、信任度与金字塔崩网等均已出现可核事实。
- 来源：E466–E473、E491等；以各事件实际开放为准。
- 机制：①区块终端金字塔承载光宴残片（C1361）；②终端集群≥200与七神算力分流（C1363）；③替身捏造与蜂巢入队（C1364）；④可塑性记忆与信任度（C1367）；⑤九十八意志分裂互吞（C1368）；⑥金字塔反向过载崩网（C1385）。
- 限制与代价：轻增量不修改既有67%完成度机体定义；意识残片≠诞所半成品机体自动补全。
- 未知项：见各子条。
- 禁止外推：不把多终端写成已完成单一完整光宴神格；不提前开放E492之后事实。
<%_ } _%>
