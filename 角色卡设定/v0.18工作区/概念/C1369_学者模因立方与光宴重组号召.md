<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E474.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E475.状态", { defaults: "未触发" }))) { _%>
# C1369·学者模因立方与光宴重组号召

- 类别：模因／集会
- 未知项：原文未给出可独立核验的闭环边界，缺证处保持开放。
- 事实适用：E474抛出模因立方与集会空间入口；E475展开殖装与重组号召。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E474.状态", { defaults: "未触发" }))) { _%>
## E474
- 定义：中高级光宴可被拖入虚无数据空间集会；对面同等等级光宴＝所有中级以上意识；同级约39、更前三名＝最高级光宴终端。
- 来源：E474
- 机制：视野可共享；命令下来时集会召开。
- 禁止外推：不把集会写成已完成全员重组。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E475.状态", { defaults: "未触发" }))) { _%>
## E475
- 定义：学者制数据模因／格式模因病毒立方体；殖装入体，目标再动即起效；感染不致死→大规模格式与更新、人格数据回分裂最初→「合格的女儿」。
- 来源：E475
- 机制：高阶识破「何不散播」；灭绝者承久存网内不可控，故须确认行动后再布罗网；长时间寄生光宴数据体也可能对她们起作用。高阶自吸一部分后裂数十份射向在场每个光宴——「只要还有一个意识尚存，就算被格式也依然是光宴」。行动成功则提恢复权限荣归七神；失败则无人能帮。
- 限制与代价：重组＝号召／未来目标，非当场完成态；模因入体后视野马赛克为起步副作用。
- 禁止外推：不把权限恢复承诺写成已表决通过。
<%_ } _%>
<%_ } _%>
