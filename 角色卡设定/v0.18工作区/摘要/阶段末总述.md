<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E435.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E436.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E437.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E438.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E439.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E440.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E441.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E442.状态", { defaults: "未触发" }))) { _%>
# 西海突围与王戟继承

无序撕开八旗封锁后，真身转入深海王庭与紫罗兰葬所；王座对线揭开大君遗产与沉眠者病变，王戟落入可使用窗口。王兽净化、母树双法则留种与第二渡鸦交付把西海决战推到可反击边缘，芬里斯赴死与康斯坦丁复苏并列为已付代价。 来源：E435、E436、E437、E438、E439、E440、E441、E442。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E443.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E444.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E445.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E446.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E447.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E448.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E449.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E450.状态", { defaults: "未触发" }))) { _%>
# 失却之海到天空城权柄

捏爆渡鸦夺控失却之海并覆灭导航者后，战线转入天空之城集体升格与言出法随对决。王骑接戟隔绝外宇通道，必败战争与时间议题公开；左左亲密惩罚只结算机制因果。王兽重誓与三叉戟永久归属打开内界与搬空天空城的窗口，黑夜城承接坠落遗产。 来源：E443、E444、E445、E446、E447、E448、E449、E450。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E451.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E452.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E453.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E454.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E455.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E456.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E457.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E458.状态", { defaults: "未触发" }))) { _%>
# 戟内训练与七神开战

艾泽法拉救援与三叉戟内界确认狱卒转化可能；黄金泰坦与灵能课把血肉／灵能底子推到可测。死兆评定未过君王门槛即接七神宣战，地狱火走廊首战以坠舰收束，纳米集群风暴把战争假说从宏观清扫改写为微观灾难。 来源：E451、E452、E453、E454、E455、E456、E457、E458。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E459.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E460.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E461.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E462.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E463.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E464.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E465.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E466.状态", { defaults: "未触发" }))) { _%>
# 纳米囚笼与献祭反转

战略撤退转入戟内整备与火山潜入；纳米工厂与巨像细胞把神教能力升到建厂级天罗地网。数字之神六棱笼镇压巨像，数学魔方困住救援，外宇献祭线把受试者标成祭品。饲养无序替换人牲后外部代行者反向脱困，战场无序成为定时灾难，幽灵潜入打开光宴终端入口。 来源：E459、E460、E461、E462、E463、E464、E465、E466。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E467.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E468.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E469.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E470.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E471.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E472.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E473.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E474.状态", { defaults: "未触发" }))) { _%>
# 光宴内应与七神内战

伪造过去与原子革命叙述把光宴从肢解话术转为可谈判意志；替身入队后七十二小时协议与巨像诱饵线索并行。龙首谎言公开内战，九十八分裂与可塑性记忆抬升中级枢纽权限，真实碎片与两枚光宴选择把学者模因罗网推入可布置窗口。 来源：E467、E468、E469、E470、E471、E472、E473、E474。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E475.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E476.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E477.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E478.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E479.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E480.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E481.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E482.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E483.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E484.状态", { defaults: "未触发" }))) { _%>
# 模因爆发与以太成型

光宴重组埋下格式模因；复燃火种与涂鸦暗号把银色幻想从猎杀线偏到会合线。以太狂潮吞噬巢都，一号智脑重创可携；田园情报销毁后伪身陷阱激活全网模因，天降援救引爆火莲瘫痪。以太水银吞噬学者并开价将军灾厄，三方汇合在智脑撕票下被迫有限合作。 来源：E475、E476、E477、E478、E479、E480、E481、E482、E483、E484。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E485.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E486.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E487.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E488.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E489.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E490.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E491.状态", { defaults: "未触发" })) && ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E492.状态", { defaults: "未触发" }))) { _%>
# 围猎巨像与开放终点

并肩逃杀中遗产钥匙暴露，以太分身升至可俘获质量并被拖入奇异世界。定位矩阵与最终防火墙把巨像线逼到必须现实破门；机械战争打进上传生命线，灭绝级阵列与全面诅咒崩坏上层终端。门前确认守门者为以太、容器空、学者远程锁场，停在「吃了」——胜负与巨像尽噬未闭合。 来源：E485、E486、E487、E488、E489、E490、E491、E492。
<%_ } _%>
