<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E499.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E506.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E508.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E509.状态", { defaults: "未触发" }))) { _%>
# 增量说明
- 类别：既有概念增量
- 定义：承接既有条目，本文件仅登记增量。
- 来源：E499／E506／E508／E509
- 机制：仅追加下列事件门槛内已出现事实；既有条目不在此重录。
- 限制与代价：未完成或未变形的事件事实不得提前引用。
- 未知项：见各事件小节。
- 禁止外推：林恩对白、主动动作、决定、判断、内心与记忆由玩家独占。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E499.状态", { defaults: "未触发" }))) { _%>
# C877·界门与地狱锁链·E499
- 类别：界门
- 定义：真界门未开；假开门浪涛用于调走半边以太原子狂潮。
- 来源：E499
- 机制：界门锁前置与力量消耗未满足时强行示形只会招来定位与反噬。地狱锁链本场未作新的远程锁神展示。
- 限制与代价：假门代价是半魂。
- 未知项：锁链对以太例外条款的现场验证。
- 禁止外推：禁止写成黑夜城界门已接通。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E506.状态", { defaults: "未触发" }))) { _%>
# C877·界门与地狱锁链·E506
- 类别：界门
- 定义：现实双开门：先回奇异世界再开黑夜城；艾雯降阶抢未稳通道；百万公里级界门负荷成立。
- 来源：E506
- 机制：①双开门顺序固定：奇异世界→黑夜城。②未稳通道可被降阶者抢占。③百万公里级距离使负荷与暴露同步上升。④夜医超负荷干枯灌注＋蚀痕汇流可作供能侧写。
- 限制与代价：负荷可导致执行者干枯／降阶风险。
- 未知项：锁链是否同步延长到双开门路径。
- 禁止外推：禁止写成双开门无代价或已永久固定。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E508.状态", { defaults: "未触发" }))) { _%>
# C877·界门与地狱锁链·E508
- 类别：界门
- 定义：五台光门吸噬作为界门／光门在封印向的应用；抽离以太流体。
- 来源：E508
- 机制：与母树枷锁、格式塔锁核同场；界门体系进入「封印门」而非「逃生日」。
- 限制与代价：封印门对以太特化，不保证对其它存在同样有效。
- 未知项：光门关闭后的残压。
- 禁止外推：禁止写成界门已把以太永久关死。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E509.状态", { defaults: "未触发" }))) { _%>
# C877·界门与地狱锁链·E509
- 类别：界门
- 定义：无坐标界门仍可被曲率追迹；边境断层成为门／撞击前线。
- 来源：E509
- 机制：失联七千年呼唤与银色绝对秩序聚合物撞击同场；开门即暴露。
- 限制与代价：边境开门极危险。
- 未知项：地狱锁链在边境的最大约束对象。
- 禁止外推：禁止写成无坐标即绝对安全。
<%_ } _%>

<%_ } _%>
