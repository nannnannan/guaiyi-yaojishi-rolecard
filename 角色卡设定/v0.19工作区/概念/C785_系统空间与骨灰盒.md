<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E500.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E505.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E507.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E537.状态", { defaults: "未触发" }))) { _%>
# 增量说明
- 类别：既有概念增量
- 定义：承接既有条目，本文件仅登记增量。
- 来源：E500／E504／E505／E507／E537
- 机制：仅追加下列事件门槛内已出现事实；既有条目不在此重录。
- 限制与代价：未完成或未变形的事件事实不得提前引用。
- 未知项：见各事件小节。
- 禁止外推：林恩对白、主动动作、决定、判断、内心与记忆由玩家独占。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E500.状态", { defaults: "未触发" }))) { _%>
# C785·系统空间与骨灰盒·E500
- 类别：空间
- 定义：半魂获系统空间权重，速将二人转入系统空间再入三叉戟奇异世界；与莫比乌斯环路套娃。
- 来源：E500
- 机制：本体在奇异世界则系统空间坐标随之转移；离奇异世界回系统空间、再出仍回奇异世界，现实链接坐标可消失。系统空间仍是中转容器，不是战场本体。
- 限制与代价：权重依赖半魂；环路细节并引莫比乌斯专条。
- 未知项：骨灰盒本场是否动用。
- 禁止外推：禁止写成系统空间可直接抹杀以太本体。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" }))) { _%>
# C785·系统空间与骨灰盒·E504
- 类别：空间
- 定义：试验与巨构设计期间，系统空间仍作临时收纳／回避坐标，不改写其基本规则。
- 来源：E504
- 机制：银色圆球与时间停滞立场等外场手段为主；系统空间只保持可进入性。
- 限制与代价：外场试验失败不自动损坏系统空间。
- 未知项：与恒星矩阵的接口是否存在。
- 禁止外推：禁止把系统空间写成恒星牢笼的替代品。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E505.状态", { defaults: "未触发" }))) { _%>
# C785·系统空间与骨灰盒·E505
- 类别：空间
- 定义：破界前后系统空间可作短避难，但不解决奇异基石≠现实物质的造机约束。
- 来源：E505
- 机制：棺椁卡缝破界对面是乱流／夹层虚空；系统空间不能替代破界通道。
- 限制与代价：夹层虚空不可当长期居所。
- 未知项：骨灰盒与破界的联动。
- 禁止外推：禁止写成系统空间已连通主宇宙任意坐标。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E507.状态", { defaults: "未触发" }))) { _%>
# C785·系统空间与骨灰盒·E507
- 类别：空间
- 定义：暂时升神与原子风暴期间，系统空间仍可收容非战斗资产，不提供神降级输出。
- 来源：E507
- 机制：神降·烬灭与原子革命风暴是外场事实；系统空间不放大升神。
- 限制与代价：躲入系统空间不等于脱离风暴影响的一切后果。
- 未知项：风暴中进入系统空间的稳定性。
- 禁止外推：禁止把系统空间写成无敌避难所。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E537.状态", { defaults: "未触发" }))) { _%>
# C785·系统空间与骨灰盒·E537
- 类别：空间／封口
- 定义：系统空间五花大绑封口（已见真容）。
- 来源：E537
- 机制：主母之战中，系统空间可被外部高位存在察觉并封口捆死；「已见真容」表示对系统空间内部／权限不再是完全黑箱。封口期间难作常规出入。
- 限制与代价：封口可造成战术失能；以命相搏等外场手段不自动解封。
- 未知项：封口解除条件与所见真容的具体内容。
- 禁止外推：禁止写成系统空间已被永久摧毁或永久接管。
<%_ } _%>

<%_ } _%>
