@@private
# 概念·神格/时间机制·碎裂神格·错误道路（事件["E574","E575","E576"]）

- 类别：神格/时间机制
- 事实门槛：仅在事件事实对应的事件进入“完成”或“变形”，且聊天正文实际出现该事实时引用；未形成的事件事实不进入当前状态。
- 定义：碎裂神格与错误道路是异时末日中神格结构损毁、因果路径偏移后的状态。
- 来源：本概念的具体来源由标题事件数组中的事件记录提供，事件事实由下方动态门控逐条输出。
- 机制：神格在错误路径中失去统一核心，分裂为多个不完整方向；时间冻结暂时保存状态，迷雾高塔提供观察节点。
- 补充事实：碎裂神格不等同于过去神王、血枢残魂或终焉之主。
- 限制与代价：强行重组会产生权柄冲突、记忆断裂和时间回声，可能扩大末日。
- 未知项：碎裂原因、碎片数量、复原路径和错误道路终点仍未确定。
- 禁止外推：林恩的对白、动作、心理与记忆由玩家决定；事件未完成或变形时，不把预设路径写成已经发生的事实。

<%_
const eventFactMap = {
  "E574": {
    "完成": "E574·错误道路与碎裂神格：枯骨身份线索、黑水遗址、错误道路和初诞者唯一性被确认",
    "变形": "E574·错误道路与碎裂神格：路线转为吸收碎裂神格、改走另一个行动方路线"
  },
  "E575": {
    "完成": "E575·异时末日与时间冻结：迷雾海的时间异变进一步升级；碎裂神格与错误道路是异时末日中神格结构损毁、因果路径偏移后的状态",
    "变形": "E575·异时末日与时间冻结：路线转为继续沉入异时世界追查时间尽头；碎裂神格与错误道路是异时末日中神格结构损毁、因果路径偏移后的状态"
  },
  "E576": {
    "完成": "E576·高塔与破碎时间线：迷雾海时间异常与高塔建立关联；碎裂神格与错误道路是异时末日中神格结构损毁、因果路径偏移后的状态",
    "变形": "E576·高塔与破碎时间线：路线转为返回高塔消失处追踪背影；碎裂神格与错误道路是异时末日中神格结构损毁、因果路径偏移后的状态"
  }
};
const activeEventFacts = [];
for (const eventId of Object.keys(eventFactMap)) {
  const state = getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" });
  if (["完成", "变形"].includes(state)) {
    const entry = eventFactMap[eventId];
    const fact = typeof entry === "string" ? entry : entry[state];
    if (fact) activeEventFacts.push(fact);
  }
}
_%>
<%_ if (activeEventFacts.length > 0) { _%>
## 事件事实汇总
<%- activeEventFacts.map((fact) => "- " + fact).join("\n") %>
<%_ } _%>
