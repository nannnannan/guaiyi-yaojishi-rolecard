@@private
# 概念·等级/代价·半步神位与法则反噬（事件["E542","E544","E546"]）

- 类别：等级/代价
- 事实门槛：仅在事件事实对应的事件进入“完成”或“变形”，且聊天正文实际出现该事实时引用；未形成的事件事实不进入当前状态。
- 定义：半步神位与法则反噬是根源训练者接近神位时出现的过渡状态，以规则掌握换取短时高位能力，并把法则压力反向施加给承载者。
- 来源：本概念的具体来源由标题事件数组中的事件记录提供，事件事实由下方动态门控逐条输出。
- 机制：承载者以根源训练积累规则位置，在战斗中调用高位力量；越阶结果沿肉身、神魂和因果链产生反噬。
- 补充事实：半步神位不等于完整神位，不自动授予神王、初诞者或终焉身份。
- 限制与代价：持续时间、能量和承载体有限，反噬会造成神魂损伤、意识撕裂和能力回落。
- 未知项：等级、突破条件、反噬上限以及与心灵之海、三招赌约的关系仍未确定。
- 禁止外推：林恩的对白、动作、心理与记忆由玩家决定；事件未完成或变形时，不把预设路径写成已经发生的事实。

<%_
const eventFactMap = {
  "E542": {
    "完成": "E542·法则反噬与半步神位：康斯坦丁成为半步神位战力",
    "变形": "E542·法则反噬与半步神位：优先修复银色幻想、取消船长决斗；半步神位与法则反噬是根源训练者接近神位时出现的过渡状态，以规则掌握换取短时高位能力，并把法则压力反向施加给该对象"
  },
  "E544": {
    "完成": "E544·八成根源围猎：根源军团出现集体控制风险；半步神位与法则反噬是根源训练者接近神位时出现的过渡状态，以规则掌握换取短时高位能力，并把法则压力反向施加给该对象",
    "变形": "E544·八成根源围猎：路线转为主动投降并接受根源审判；半步神位与法则反噬是根源训练者接近神位时出现的过渡状态，以规则掌握换取短时高位能力，并把法则压力反向施加给该对象"
  },
  "E546": {
    "完成": "E546·嘲讽模式与根源拔刀：多名根源初步理解自身法则；半步神位与法则反噬是根源训练者接近神位时出现的过渡状态，以规则掌握换取短时高位能力，并把法则压力反向施加给该对象",
    "变形": "E546·嘲讽模式与根源拔刀：主动撤离并保留黑袍身份；半步神位与法则反噬是根源训练者接近神位时出现的过渡状态，以规则掌握换取短时高位能力，并把法则压力反向施加给该对象"
  }
};
const activeEventFacts = [];
for (const eventId of Object.keys(eventFactMap)) {
  const state = getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" });
  if (["完成", "变形"].includes(state)) {
    const entry = eventFactMap[eventId];
    const fact = typeof entry === "string" ? entry : entry[state];
    if (fact) activeEventFacts.push(fact);
  }
}
_%>
<%_ if (activeEventFacts.length > 0) { _%>
## 事件事实汇总
<%- activeEventFacts.map((fact) => "- " + fact).join("\n") %>
<%_ } _%>
