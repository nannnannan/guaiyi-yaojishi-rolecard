@@private
# 概念·机制·同生共死与极端反腐化机制（事件["E668","E669","E670"]）

- 类别：机制
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：同生共死与极端反腐化机制是高位污染战场中的集体承压与隔离方案。它把成员、空间、信标和法则节点组织为可分流的防线，具体责任按事件事实记录。
- 来源：E668至E670记录远征分流、腐化疑云、银色幻想告别、画家资源和后方防线安排。
- 机制：队伍按任务和本源分流，共享战报而不共享全部意识；隔离、封闭和支援会分担污染与资源压力。
<%_
const eventFactMap = {
  "E668": "后方获得极端反腐化机制，小丑与伊甸下落暂时中断，神界远征进入最后准备。",
  "E669": "内部信任问题转为无法检测的意识风险，林恩保留以太作为后续防护资源。",
  "E670": "林恩拥有跨界资源与艾维儿载体，画家承担引开敌人的主要压力。"
};
const eventFactMapTransform = {
  "E668": "变形时，林恩取消远征或要求小丑等人留守；现场位于黑夜城、三叉戟世界前置区域、无序远征通道。",
  "E669": "变形时，林恩直接取走以太本源或暂停远征；现场位于黑夜城、银色幻想修复室、创造神格内层。",
  "E670": "变形时，林恩留在苍蓝世界研究资源或把龙蛋交给其他人；现场位于画家苍蓝世界、紫罗兰法师塔、远征入口。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：集体承压会把伤害、资源消耗和决策压力分摊给更多单位，无法保证所有成员安全。过度隔离也会造成信息断裂和信任损耗。
- 未知项：机制的完整规则、污染判定标准、最大承载人数和能否对终焉级力量生效仍未确定。
- 禁止外推：不把同生共死写成真实灵魂绑定或成员必然同死，不替玩家决定是否采用分流、封闭和承压。
