@@private
# 概念·存在·因果化身（事件["E675","E676","E677"]）

- 类别：存在
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：因果化身是因果神王从本体分离的行动形态。它在局部时间和历史区域执行因果控制，化身与本体状态按事件事实分开记录。
- 来源：E675至E677记录因果化身出现、峡谷封锁、银色太阳介入和化身终止。
- 机制：因果化身以因果线、路径和记忆样本维持局部控制；节点被切断可终止化身，但不自动影响本体。
<%_
const eventFactMap = {
  "E675": "林恩把因果化身与银色太阳放在同一追踪链上，进入峡谷调查时间异常。",
  "E676": "林恩获得直接对抗因果化身的机会，时间神王开始以见证者身份介入。",
  "E677": "林恩清除一个直接威胁，却确认因果神王仍然存在，银色通道把他引向时间神王。"
};
const eventFactMapTransform = {
  "E675": "变形时，林恩先护送老者或放弃追踪；现场位于旧世林地小世界、命运神国村落。",
  "E676": "变形时，林恩优先护送小世界居民；现场位于旧世林地峡谷、时间异常区。",
  "E677": "变形时，林恩保留化身审问或优先修复小世界；现场位于旧世林地小世界、封闭峡谷、时间丝线阵。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：化身的终止只处理当前分离形态，不能自动消灭因果神王本体。化身能力受封闭峡谷、神国残余和时间通道限制。
- 未知项：因果化身的数量、分离条件、记忆是否回传本体和是否存在备用化身仍未确定。
- 禁止外推：不把因果化身写成因果神王本体死亡，不把局部因果剪裁扩展为全时间线任意改写。
