@@private
# 概念·机制·时间塌陷（事件["E650","E651","E652"]）

- 类别：机制
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：时间塌陷是时间结构在高位干预或旧日压力下发生断裂的空间现象。它可以让不同时间位置短暂重叠，具体地点、落点和后果按事件事实记录。
- 来源：E650至E652记录高塔深层时间异常、林恩重伤坠入时间之外、左左冻结、时间尽头和未来林恩线索。
- 机制：时间压力会使空间距离、历史顺序和身体状态失去一致；裂隙、梦境和时间通道可能保存或切断联系，持续时间与返回条件按事件事实记录。
<%_
const eventFactMap = {
  "E650": "林恩与左左的联系被时间塌陷撕开，时间尽头和未来残影进入可见范围。",
  "E651": "林恩失去左左这一最强辅助，时间之外冻结成为后续寻找时间神格的核心动机。",
  "E652": "时间战争从平行世界冲突改写为单一时间线的自我推演，林恩承担重铸因果的使命。"
};
const eventFactMapTransform = {
  "E650": "变形时，林恩主动放弃诱敌并保护高塔残民；现场位于幽冥海高塔深层、时间异常区。",
  "E651": "变形时，林恩留在时间之外寻找左左；现场位于时间之外虚空、时间长河入口。",
  "E652": "变形时，林恩留在时间尽头守墓；现场位于幽冥海高塔孤岛、时间尽头高塔坟冢。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：时间塌陷不是稳定传送门，持续时间、落点和返回路径都受高塔结构、金色梦境和时间长河影响。塌陷只改变当前时间关系，不能自动修复历史。
- 未知项：高塔旧日结构的制造者、塌陷能否重复、时间之外的完整范围和塌陷后遗留的时间裂口仍未确定。
- 禁止外推：不把时间塌陷写成可随意使用的穿越技能，不把时间残影写成真实复活。林恩的行动和回应由玩家输入决定。
