@@private
# 概念·地点·蓝星遗址（事件["E704","E705"]）

- 类别：地点
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：蓝星遗址是蓝星毁灭后遗留的本源与文明残迹地点。遗骸、光源、训练和现实位置按事件事实记录。
- 来源：E704至E705记录蓝星遗址、以太遗骸、苍白耀阳、闭关吸收和过去线训练。
- 机制：遗址以残骸、隔离光源和时间训练承载吸收过程，外部战争不会因闭关暂停，吸收会改变承载压力。
<%_
const eventFactMap = {
  "E704": "林恩进入长时间成长，主宇宙在没有他直接指挥的情况下迎来十五年战争。",
  "E705": "林恩获得稳定罪火与梦境显化基础，为日后对抗魇王积累条件。"
};
const eventFactMapTransform = {
  "E704": "变形时，林恩先回黑夜城安排军团或停止吸收以太；蓝星遗址中的以太遗骸仍在现场。",
  "E705": "变形时，林恩停止训练并返回现实；蓝星遗址中的以太吸收训练线随之中断。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：遗址已失去完整文明结构，残骸数量和以太安全性有限。闭关不能暂停外部战争，吸收也可能改变林恩本源。
- 未知项：蓝星毁灭的全部残留、以太遗骸完整规模、遗址是否含其他幸存者和战后是否重新利用仍未确定。
- 禁止外推：不把蓝星遗址写成仍有完整城市和文明，不把以太遗骸吸收写成无代价升级。
