@@private
# 概念·机制·十分钟因果铁律（事件["E697","E698","E699"]）

- 类别：机制
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：十分钟因果铁律是神王界战场中的限时因果规则。它压缩本源、神疆和承载方式的选择窗口，起点、作用和结果按事件事实记录。
- 来源：E697至E699记录魇王三人组破界、因果铁律、画家化身争取时间、终焉之柱击穿和本源熔铸三叉戟。
- 机制：铁律把时间窗口固定并随推进削弱保护层，外部干预可改变局部结果；倒计时不自动决定玩家选择。
<%_
const eventFactMap = {
  "E697": "林恩只有极短时间处理初诞者本源，画家化身支援也被迫进入战场。",
  "E698": "林恩被迫在初诞者路线与三叉戟承载路线之间作出关键选择。",
  "E699": "林恩获得接近神王顶层的武器权柄，但不能长期持有，否则会持续引来灾厄。"
};
const eventFactMapTransform = {
  "E697": "变形时，林恩撤离本源并放弃神王界；现场位于神王界中心、金色神疆、历史法则烙印战场。",
  "E698": "变形时，林恩放弃本源并带队撤离；现场位于神王界中心、因果铁律覆盖区、金色最后防线。",
  "E699": "变形时，林恩立即追击魇王或先处理信标；现场位于神王界中心、终焉之柱落点、金色法则海。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：铁律作用范围限于当前神王界战场，不能直接改写全部时间。倒计时的起点、暂停条件和因果神王本体状态需要分别记录。
- 未知项：十分钟是否为固定规则、其他神王能否建立类似铁律和铁律结束后的残留代价仍未确定。
- 禁止外推：不把十分钟铁律写成通用时间停止，不替林恩决定本源承载方式。
