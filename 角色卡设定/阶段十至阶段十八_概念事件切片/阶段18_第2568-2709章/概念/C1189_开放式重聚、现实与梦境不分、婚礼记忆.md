@@private
# 概念·状态/关系·开放式重聚、现实与梦境不分、婚礼记忆（事件["E798"]）

- 类别：状态/关系
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：开放式重聚、现实与梦境不分、婚礼记忆记录一条终局画面及其解释边界，具体内容只由E798门控事件事实提供。
- 来源：本条具体事实均由E798门控事件提供，未满足门槛时不展开画面内容。
- 机制：记忆、时间权柄和梦境结构可以编排画面；画面性质和参与者状态按E798事实分别判断。
<%_
const eventFactMap = {
  "E798": "终焉故事在开放式日常画面中结束，宴会重聚是否属于现实保留解释空间。"
};
const eventFactMapTransform = {};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：E798之后没有后继事件，终局画面不能向后推导新的冒险或角色状态；其解释以E798门控事实为准。
- 未知项：E798门控事实中的回返身体、参与者状态和画面性质仍未确定。
- 禁止外推：不把开放式重聚改写成确定复活或确定幻觉，不创建终局后事件，不替林恩书写回返后的对白、动作、记忆和内心。
