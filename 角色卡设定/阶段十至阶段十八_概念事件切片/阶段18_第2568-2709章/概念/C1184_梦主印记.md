@@private
# 概念·物品/特殊设定·梦主印记（事件["E787","E788"]）

- 类别：物品/特殊设定
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：梦主印记是从远古三位一体之主记忆中取得的存在痕迹。印记内容、承载和影响按事件事实记录。
- 来源：E787至E788记录梦主记忆展开、永恒分裂、终焉另一半、林恩吸收印记和返回当前时间。
- 机制：印记沿梦境、三位一体核心和时间桥映射历史记忆；吸收可能增加信息也可能增加重叠与负荷。
<%_
const eventFactMap = {
  "E787": "林恩获得梦境世界本质与终焉来源，时间战争的最终目标变得清晰。",
  "E788": "林恩进入寻找十万渡鸦与最后载体的悖论终战，织梦留在过去时间。"
};
const eventFactMapTransform = {
  "E787": "变形时，林恩拒绝记忆或让左左切断回声；现场位于旧世永恒记忆、梦主分裂场、苦痛轮回层。",
  "E788": "变形时，林恩留在冬泉谷或先修复身体；现场位于冬泉谷、时间之外、当前时间入口。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：印记不等于梦主完整复生，吸收过程可能覆盖当前意识或引发终焉反噬。印记只能提供记忆和结构，不能替代林恩的行动选择。
- 未知项：印记能否被移除、是否留有梦主控制、完整信息比例和融合后的残留仍未确定。
- 禁止外推：不把梦主印记写成神格，不把吸收印记写成林恩失去玩家主权，也不把印记直接写成终焉控制器。
