@@private
# 概念·能力/关系·仙月丝线领域与人偶家传承（事件["E757"]）

- 类别：能力/关系
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：仙月丝线领域与人偶家传承是人偶家以丝线、记忆和家族本源维持的领域与传承机制。领域、成员、牺牲和延续状态按事件事实记录。
- 来源：E757记录仙月进入丝线领域、承接人偶家力量、守住最后墙面和小小牺牲前的信号。
- 机制：丝线连接人偶家成员、仙月记忆和战场位置，能够形成防护或保存痕迹；连接强度和承载代价按事件记录。
<%_
const eventFactMap = {
  "E757": "林恩的精神防线被现实死亡击穿，隐藏的小小即将成为直接牺牲。"
};
const eventFactMapTransform = {
  "E757": "变形时，林恩关闭梦境声音或让仙月撤离墙面；仙月丝线领域的最后墙面停止继续承接梦境弦奏。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：丝线数量、承载范围和持续时间有限，不能恢复已消失生命，也不能单独阻止终焉之火。
- 未知项：仙月的战后状态、人偶家力量是否留存、丝线领域能否重建和与梦境重聚的关系仍未确定。
- 禁止外推：不把仙月丝线写成永久复生工具，不把人偶家传承写成仙月已经完全取代人偶家。
