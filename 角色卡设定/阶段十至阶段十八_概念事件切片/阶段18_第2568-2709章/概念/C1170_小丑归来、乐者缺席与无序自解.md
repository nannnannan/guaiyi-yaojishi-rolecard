@@private
# 概念·人物/关系·小丑归来、乐者缺席与无序自解（事件["E743","E744"]）

- 类别：人物/关系
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：小丑归来、乐者缺席与无序自解记录无序体系中小丑、乐者和无序力量的状态变化。各自身份与回归条件按事件事实记录。
- 来源：E743至E744记录小丑归来、无序生物自解、黄金时代、王座黑裂和终焉苏醒信号。
- 机制：小丑与乐者的信号、记忆和无序区域相互关联，无序自解会改变回响和控制；寻找、回声与正式回归分别结算。
<%_
const eventFactMap = {
  "E743": "旧同伴记忆回到因果网络，终焉苏醒信号从黑色裂纹中进一步显现。",
  "E744": "信仰帝国进入最终牺牲前夜，林恩即将登临最后的黑暗，艾雯的孤独预感成为终局伏笔。"
};
const eventFactMapTransform = {
  "E743": "变形时，林恩追查乐者或拆除黄金时代；小丑、乐者残响与黄金时代王庭成为追查对象。",
  "E744": "变形时，林恩先撤离信仰帝国或拒绝晋升；王庭仍是小丑留下八音盒的现场。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：小丑归来只说明其重新出现，不代表乐者已经回归或无序危机已经解决。无序自解的速度和范围随终焉信号变化。
- 未知项：小丑在无序深空的经历、乐者真实状态、自解原因和归来后的长期立场仍未确定。
- 禁止外推：不把小丑归来写成乐者回归，不把无序自解写成全部无序生物死亡，也不提前加入伊甸的终局参与。
