@@private
# 概念·状态/历史·孤王时代与唯一活动者（事件["E760","E761"]）

- 类别：状态/历史
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：孤王时代与唯一活动者记录终焉战争后活动者大幅减少的时间状态。林恩、时间外区域和剩余行动者按事件事实记录。
- 来源：E760至E761记录幸存者聚居点、最后幸存者死亡、残存宇宙、黑夜城重建和时间长河阻隔。
- 机制：因果燃烧和终焉攻击会削减可行动单位，时间外空间保存少量行动窗口；唯一活动者不自动意味着其他存在全部消失。
<%_
const eventFactMap = {
  "E760": "林恩确认自己成为唯一活动者，重建与时间外行动成为新的长期路线。",
  "E761": "林恩把黑夜城转为时间外行动基地，轮回神王的牺牲将成为突破门槛。"
};
const eventFactMapTransform = {
  "E760": "变形时，林恩继续寻找幸存者或离开残存宇宙；现场位于残存宇宙、幸存者聚居点、废墟星球。",
  "E761": "变形时，林恩放弃重建或改为守城；现场位于残存宇宙、重建黑夜城、时间长河外缘。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：唯一活动者描述的是当前仍能持续行动的主体，不等同于宇宙中所有灵魂已经彻底消失。黑夜城重建只是记忆和结构恢复。
- 未知项：孤王时代持续时间、其他微弱生命、黑夜城完整重建程度和记忆层的现实效力仍未确定。
- 禁止外推：不把孤王时代写成林恩已经失去全部情感和记忆，不替林恩决定继续守护或进入时间之外。
