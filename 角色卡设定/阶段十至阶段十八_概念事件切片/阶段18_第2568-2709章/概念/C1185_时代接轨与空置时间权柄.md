@@private
# 概念·机制/权柄·时代接轨与空置时间权柄（事件["E790","E791","E792","E793"]）

- 类别：机制/权柄
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：时代接轨与空置时间权柄是过去、现在和未来时间层发生重合后的结构。权柄空置、承接和历史影响按事件事实记录。
- 来源：E790至E793记录最后载体、未来通道、时代合流、时间权柄空门、轮回牺牲和左左承接空置权柄。
- 机制：时代接轨压缩多个时间层，空置权柄在可承载主体之间转移；转移、记忆和桥接风险分别结算。
<%_
const eventFactMap = {
  "E790": "林恩进入时代接轨与时间权柄争夺，空白实体掌握最后历史载体。",
  "E791": "林恩进入时间权柄与轮回遗志的最终争夺，终焉倒计时开始。",
  "E792": "轮回承担最后攻击，左左获得空置时间权柄，林恩取得短暂复苏窗口。",
  "E793": "轮回完成梦主遗志的最后牺牲，林恩取得进入终焉核心的资格。"
};
const eventFactMapTransform = {
  "E790": "变形时，林恩先救画家或攻击空白实体；现场位于冬泉谷、紫罗兰旧地、时间终点边缘。",
  "E791": "变形时，林恩先救画家或撤回当前；现场位于未来时代、时间权柄通道、时代合流区。",
  "E792": "变形时，林恩直接夺取时间权柄或保护轮回；现场位于时代合流区、时间权柄空门、轮回桥接战场。",
  "E793": "变形时，林恩追随轮回或先救左左；现场位于时代合流区、时间终点、轮回挡劫战场。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：时代接轨会造成记忆重叠、身体消散、因果错位和时间桥断裂。空置权柄的临时承接不等于永久继承。
- 未知项：时间权柄原始持有者、空门完整功能、时代合流能否逆转和左左最终保留程度仍未确定。
- 禁止外推：不把时代接轨写成普通传送，不把左左承接空权柄写成时间神王身份转移。
