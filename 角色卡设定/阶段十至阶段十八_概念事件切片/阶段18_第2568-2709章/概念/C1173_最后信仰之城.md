@@private
# 概念·地点·最后信仰之城（事件["E750","E751","E752"]）

- 类别：地点
- 事实门槛：仅在标题事件数组所列 E 编号中的相关事件状态为“完成”或“变形”，且正文实际出现本条对象、状态或关系时引用；未触发的后续形态保持未成立。
- 定义：最后信仰之城是终局战争中承载信仰、幸存者和王座防线的城市区域。其防线、燃料和沦陷状态按事件事实记录。
- 来源：E750至E752记录最后信仰之城、城门防守、女巫神、泰坦、小丑、信徒撤离和城市失守。
- 机制：城市通过王座、圣堂残余和参与者行动维持短时防护；噩梦、牺牲和黑火消耗会逐步改变结构。
<%_
const eventFactMap = {
  "E750": "女巫神与泰坦成为最后城池的主要牺牲者，王座获得短暂续燃时间。",
  "E751": "信仰城失去最后屏障，小丑与泰坦进入牺牲式战斗，王座获得短暂信仰增幅。",
  "E752": "小丑与泰坦进入牺牲状态，最后信仰城失守，黑夜城内城承受直接攻击。"
};
const eventFactMapTransform = {
  "E750": "变形时，玩家让泰坦优先护送居民或让林恩分流王座信仰；现场位于最后信仰之城、主宇宙内环、噩梦突破口。",
  "E751": "变形时，玩家让角色护送信徒或集中攻击柱影；现场位于最后信仰之城、城门废墟、噩梦潮前线。",
  "E752": "变形时，玩家让小丑或泰坦优先撤离；现场位于最后信仰城废墟、噩梦缺口、撤离航道。"
};
const completedEventFacts = Object.entries(eventFactMap)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "完成",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const transformedEventFacts = Object.entries(eventFactMapTransform)
  .filter(([eventId]) =>
    getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" }) === "变形",
  )
  .map(([eventId, fact]) => "- " + eventId + "：" + fact);
const activeEventFacts = [...completedEventFacts, ...transformedEventFacts].join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：城市无法长期承受噩梦潮，城墙和信仰节点会逐段失守。防守者的牺牲只能延缓突破，不能保证城内所有居民离开。
- 未知项：城市的原始名称、居民数量、撤离成功人数和战后遗址状态仍未确定。
- 禁止外推：不把最后信仰之城写成黑夜城，不把女巫神和巫神头颅拆成两个角色，也不把撤离写成无损完成。
