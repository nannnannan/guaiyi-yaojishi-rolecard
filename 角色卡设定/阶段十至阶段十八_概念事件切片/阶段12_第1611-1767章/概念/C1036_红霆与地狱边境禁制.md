@@private
# 概念·机制/诅咒·红霆与地狱边境禁制（事件["E428","E434"]）

- 类别：机制 / 远古禁制 / 毁灭神罚
- 事实门槛：E428极西起航遭遇红霆狂轰、E434灭绝令触发八旗红霆蒸海后生效。
- 定义：地狱与外部世界交界处常年盘踞的血色雷霆神罚天幕，对一切越界折跃、空间门开启与高能生命体实施毁灭性无差别打击。
- 来源：E428林恩队伍驾船穿过边境海域时遭遇红霆疯狂轰炸，全员被迫暂时避入系统空间；E434弑利亚启动八面镇旗与红霆联动蒸发万公里海水。
- 机制：
  1. 空间与肉身双重湮灭：红雷不仅作用于物理海水与肉体，更能击穿空间折跃通道，粉碎常规传送阵；
  2. 能量感应打击：目标释放的灵能波动与神性位阶越高，引落的红霆密度与杀伤力呈几何级暴涨；
  3. 避险窗口限制：林恩可利用系统空间的绝对隔离特性争取数秒战术规避窗口，但无法永久消除边境禁制。
- 补充事实：既有定义与机制的共性保持不变；后续事件事实由下方动态映射逐项门控。
- 限制与代价：正面硬抗红霆会导致护盾与机体极速过载损毁；在红霆密集区施展高阶法术极易招来连锁雷暴轰击。
- 未知项：地狱边境红霆天幕的最初布设神祇真身、是否有规避雷击的古代通行秘钥未完全查明。
- 禁止外推：林恩的对白、动作与选择由玩家主权独占；禁止违背原著设定将其描写为无消耗或脱离因果律违规生效。
<%_
const eventFactMap = {
  "E428": {
    "完成": "无尽海成为主要战场；红霆与地狱边境规则进入事件链；海底黑体和王骑战骑获得出现条件。",
    "变形": "变形态：现场不打开界门时，改用常规航行；红霆持续增强时，改为系统空间等待或从浅海落点进入；孽主不同行时，改由林恩承担飞行和侦查。"
  },
  "E434": {
    "完成": "西海成为大型封锁战场；导航者弑利亚、王兽、八面镇旗和灭绝令正式进入主线；这一段因果以林恩即将突破导航者防线的状态收束。",
    "变形": "变形态：现场不直接冲击镇旗时，改为跟随鱼群迁徙、潜入海沟或利用艾泽法拉坐标寻找结界盲区；蚀痕复制体不足时，只保留少数节点侦查；红霆提前收缩时，转入海底溶洞路线。"
  },
};
const activeEventFacts = Object.entries(eventFactMap)
  .map(([eventId, fact]) => {
    const state = getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" });
    if (!["完成", "变形"].includes(state)) return "";
    const selectedFact = fact?.[state];
    if (typeof selectedFact !== "string" || !selectedFact) return "";
    return "- " + eventId + "：" + selectedFact;
  })
  .filter(Boolean)
  .join("\n");
_%>
- 补充事实：
<% if (activeEventFacts) { %>
<%= activeEventFacts %>
<% } else { %>
当前没有满足门槛的事件事实。
<% } %>
- 限制与代价：红霆具有范围、位置和时机条件，不应被写成所有深海活动的持续天灾。系统空间的暂时规避会消耗承载和时机，跨界动作仍可能受到二次打击；禁制的边界规则不能由一次成功穿越推定完整。
- 未知项：红霆的真正触发半径、地狱边境的判定坐标、红雷持续时间、系统空间的承载上限、禁制对不同实体的差异以及是否存在稳定通行窗口仍未确定。
- 禁止外推：林恩的对白、动作、选择、判断、记忆与内心由玩家输入决定；禁止把系统空间写成能永久免疫红霆的绝对屏障。
