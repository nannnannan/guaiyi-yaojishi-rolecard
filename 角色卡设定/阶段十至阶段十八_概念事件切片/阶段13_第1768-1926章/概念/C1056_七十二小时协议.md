@@private
# 概念·关系/战时协议·七十二小时协议（事件["E470","E474"]）

- 类别：关系/战时协议
- 事实门槛：仅在事件事实对应的事件进入“完成”或“变形”，且聊天正文实际出现该事实时引用；未形成的事件事实不进入当前状态。
- 定义：七十二小时协议是围绕光宴、巨像线索和最终选择建立的限定时长战时协议。
- 来源：本概念的具体来源由标题事件数组中的事件记录提供，事件事实由下方动态门控逐条输出。
- 机制：协议以时间上限、目标范围和行动边界交换情报或暂时停火，期限内的交换会改变阵营位置，期限结束后保护失效。
- 补充事实：协议不是永久盟约，也不替代参与者的选择；光宴、巨像和机械阵营的关系依正文记录。
- 限制与代价：需要明确参与方、计时起点和违约结果，时间战争或终端故障可能造成期限认知分裂。
- 未知项：制定者、完整条款、违约惩罚、并行计时和巨像线索价值仍未确定。
- 禁止外推：林恩的对白、动作、心理与记忆由玩家决定；事件未完成或变形时，不把预设路径写成已经发生的事实。

<%_
const eventFactMap = {
  "E470": {
    "完成": "E470·七十二小时协议与巨像线索：记录七十二小时协议、巨像坐标残片、光宴吞噬成长和双方权限边界",
    "变形": "E470·七十二小时协议与巨像线索：不建立协议时，改为短时互用权限；七十二小时协议是围绕光宴、巨像线索和核心分支建立的限定时长战时协议"
  },
  "E474": {
    "完成": "E474·两枚光宴与最终选择：两枚光宴的交换条件、记忆差异、监控压力和完整化选择均被记录；七十二小时协议是围绕光宴、巨像线索和核心分支建立的限定时长战时协议",
    "变形": "E474·两枚光宴与最终选择：拒绝二选一时，改为打破数据牢笼并保留双体碎片；七十二小时协议是围绕光宴、巨像线索和核心分支建立的限定时长战时协议"
  }
};
const activeEventFacts = [];
for (const eventId of Object.keys(eventFactMap)) {
  const state = getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" });
  if (["完成", "变形"].includes(state)) {
    const entry = eventFactMap[eventId];
    const fact = typeof entry === "string" ? entry : entry[state];
    if (fact) activeEventFacts.push(fact);
  }
}
_%>
<%_ if (activeEventFacts.length > 0) { _%>
## 事件事实汇总
<%- activeEventFacts.map((fact) => "- " + fact).join("\n") %>
<%_ } _%>
