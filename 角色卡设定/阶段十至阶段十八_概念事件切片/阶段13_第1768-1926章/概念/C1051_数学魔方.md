@@private
# 概念·物品/灵魂机制·数学魔方（事件["E462","E498"]）

- 类别：物品/灵魂机制
- 事实门槛：仅在事件事实对应的事件进入“完成”或“变形”，且聊天正文实际出现该事实时引用；未形成的事件事实不进入当前状态。
- 定义：数学魔方是把灵魂分片、空间坐标和战场路径转化为可计算结构的高阶物品。
- 来源：本概念的具体来源由标题事件数组中的事件记录提供，事件事实由下方动态门控逐条输出。
- 机制：多面结构保存分片和位置关系，旋转、交换与解算会改变连接；正确解算可形成回归路径，错误解算会造成递归困锁。
- 补充事实：数学魔方与巨像囚笼、光宴碎片和时间回返存在接口，但不等同于心灵之海。
- 限制与代价：需要稳定灵魂样本、计算能力和外部坐标，分片越多，错位和丢失风险越高。
- 未知项：制作者、面数、灵魂容量、损毁分片恢复方式和原子革命关系仍未确定。
- 禁止外推：林恩的对白、动作、心理与记忆由玩家决定；事件未完成或变形时，不把预设路径写成已经发生的事实。

<%_
const eventFactMap = {
  "E462": {
    "完成": "E462·三十二魂与数学魔方：记录三十二份灵魂分流、纳米血液召唤、血肉瘟疫、数学魔方和巨像伪体",
    "变形": "E462·三十二魂与数学魔方：不进入魔方时，改为以血肉瘟疫轰击外壁；数学魔方是把灵魂分片、空间坐标和战场路径转化为可计算结构的高阶物品"
  },
  "E498": {
    "完成": "E498·数学魔方困局与战场回归：银色幻想证明至臻机体能够修改数学规则；数学魔方是把灵魂分片、空间坐标和战场路径转化为可计算结构的高阶物品",
    "变形": "E498·数学魔方困局与战场回归：路线转为停留在魔方内寻找更深层规则；数学魔方是把灵魂分片、空间坐标和战场路径转化为可计算结构的高阶物品"
  }
};
const activeEventFacts = [];
for (const eventId of Object.keys(eventFactMap)) {
  const state = getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" });
  if (["完成", "变形"].includes(state)) {
    const entry = eventFactMap[eventId];
    const fact = typeof entry === "string" ? entry : entry[state];
    if (fact) activeEventFacts.push(fact);
  }
}
_%>
<%_ if (activeEventFacts.length > 0) { _%>
## 事件事实汇总
<%- activeEventFacts.map((fact) => "- " + fact).join("\n") %>
<%_ } _%>
