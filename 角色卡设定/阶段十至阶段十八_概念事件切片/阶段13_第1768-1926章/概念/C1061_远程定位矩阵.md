@@private
# 概念·定位/通讯机制·远程定位矩阵（事件["E487","E492"]）

- 类别：定位/通讯机制
- 事实门槛：仅在事件事实对应的事件进入“完成”或“变形”，且聊天正文实际出现该事实时引用；未形成的事件事实不进入当前状态。
- 定义：远程定位矩阵是通过跨世界坐标、能量回波和意识标记锁定目标位置的定位机制。
- 来源：本概念的具体来源由标题事件数组中的事件记录提供，事件事实由下方动态门控逐条输出。
- 机制：矩阵收集血肉、灵魂、机械或规则特征，以多点回波建立坐标，结果可用于援军投放、追踪、夺取或反向设陷。
- 补充事实：定位结果不等于控制结果，巨像定位、以太夺取和援军投放是不同用途。
- 限制与代价：需要信标、目标特征和能量，跨界定位可能泄露己方坐标并暴露操作者。
- 未知项：制造者、最大距离、时间外精度、空白实体定位能力和反制方式仍未确定。
- 禁止外推：林恩的对白、动作、心理与记忆由玩家决定；事件未完成或变形时，不把预设路径写成已经发生的事实。

<%_
const eventFactMap = {
  "E487": {
    "完成": "E487·定位矩阵与援军计划：记录模因病毒定位矩阵、改写结果、巨像深层路线、上层终端破坏方案与援军配置",
    "变形": "E487·定位矩阵与援军计划：不改写病毒时，保留以太追踪风险并调整路线；远程定位矩阵是通过跨世界坐标、能量回波和意识标记锁定目标位置的定位机制"
  },
  "E492": {
    "完成": "E492·巨像位置与以太夺取：记录75号区块、三叉戟远程运兵、人偶家傀儡接应、以太规则压制、巨像空囚笼与“吃了”的现场声明；远程定位矩阵是通过跨世界坐标、能量回波和意识标记锁定目标位置的定位机制",
    "变形": "E492·巨像位置与以太夺取：不使用三叉戟远程召回时，改为从数据网络逐层突破；远程定位矩阵是通过跨世界坐标、能量回波和意识标记锁定目标位置的定位机制"
  }
};
const activeEventFacts = [];
for (const eventId of Object.keys(eventFactMap)) {
  const state = getvar(`stat_data.事件.锚点状态.${eventId}.状态`, { defaults: "未触发" });
  if (["完成", "变形"].includes(state)) {
    const entry = eventFactMap[eventId];
    const fact = typeof entry === "string" ? entry : entry[state];
    if (fact) activeEventFacts.push(fact);
  }
}
_%>
<%_ if (activeEventFacts.length > 0) { _%>
## 事件事实汇总
<%- activeEventFacts.map((fact) => "- " + fact).join("\n") %>
<%_ } _%>
