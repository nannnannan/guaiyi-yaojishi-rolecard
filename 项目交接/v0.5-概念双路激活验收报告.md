# 《诡异药剂师》v0.5 概念双路激活验收报告

- 验收日期：2026-08-09（Asia/Shanghai）
- 执行者：AGENT B / Codex
- 结论：离线与 SillyTavern 真机验收通过；保持 `candidate`，仅等待所有者确认是否发布。
- 写入边界：仅 `角色卡本体/诡异药剂师_MVU_v0.5/` 与 `项目交接/`；未改 `00-Shared/`、旧版本或其他项目。

## 最终产物

| 项目 | 结果 |
|---|---|
| 产物 | `角色卡本体/诡异药剂师_MVU_v0.5/dist/诡异药剂师_v0.5.json` |
| 世界书 | 102 条（49 既有 + ID 399 路由 + 52 概念） |
| 概念条目 | 52/52 为 `constant=false`，主关键词非空、二级关键词为空、双防递归开启 |
| 离线检查 | 4758 项通过 |
| 字节数 | 811921 |
| SHA-256 | `565787c29c6f8a9a09bcb668fd4410c9c2cf740f4f430c7e3d1e5347fd805aea` |

## 实现结论

- 关键词路完全使用酒馆原生绿灯：扫描 user+AI，`scan_depth` 与大小写等字段继承宿主全局设置，不使用二级关键词。
- 事件路由条目 ID 399 为 `constant=false`、空关键词、`@@preprocessing`，读取 MVU 当前事件并对 ±3 窗口内概念调用 `activewi(world, uid, true)`；自身不输出提示词。
- 52 条概念正文已静态化；关联事件只保存在 `extensions.tavernweave.event_ids`。事件路与关键词路命中同一世界书 UID，双命中只注入一次。
- 未修改 MVU Schema、状态栏接口或角色卡对外变量。

## 真机矩阵

环境：SillyTavern 1.17.0、酒馆助手 / JS-Slash-Runner 4.9.1。

| 用例 | 事件状态 | 关键词来源 | 目标正文计数 | 请求结果 |
|---|---|---|---:|---|
| 仅事件路 | E02（目标在 ±3） | user/AI 均无目标词 | 1 | HTTP 200 |
| 仅 user 关键词 | E20（目标在窗外） | user：`拾荒者` | 1 | HTTP 200 |
| 仅 AI 关键词 | E20（目标在窗外） | AI 历史：`拾荒者` | 1 | HTTP 200 |
| 两路均不命中 | E20（目标在窗外） | user/AI 均无目标词 | 0 | HTTP 200 |
| 两路同时命中 | E02（目标在 ±3） | user：`人皮拾荒者` | 1 | HTTP 200 |

五项实际计数为 `1/1/1/0/1`。请求捕获还证明 E20 事件状态与 AI 历史关键词确实进入对应请求，而非由测试脚本臆测命中来源。

测试时发现正式开场 AI 文本本身包含“人皮拾荒者/拾荒者”。在完全等效原生绿灯的设计下，这会合法触发关键词路；因此负例聊天仅将测试卡开场替换为不含专名的中性文本，同时保留 MVU 初始结构。这是隔离测试条件，不是修改正式产物。

## 回归与运行日志

- 最终真实生成 HTTP 200、回复 2217 字符；请求捕获中事件路目标正文出现一次。
- 当前页状态栏 iframe 成功渲染；跨窗口重新打开后聊天 ID、3 条消息和最新楼 MVU 均保持一致。
- 运行期未发现 EJS 重复声明、概念路由或世界书致命错误。
- 控制台保留 SillyTavern 宏预览噪声 `env.functions.original is not a function`；不属于 EJS/路由错误，真实请求与回复未中断。
- 多窗口下酒馆助手为单实例运行，第二窗口未重复要求状态栏 iframe；状态栏渲染在原窗口验收，跨窗口专门验收聊天与 MVU 落盘。

## 证据与清理

- 主证据：`真机证据/concept-dual-route-2026-08-09/runtime-concept-dual-route-acceptance.json`
- 五项正式请求：`真机证据/concept-dual-route-2026-08-09/request-captures/`
- 最终回归请求：`真机证据/concept-dual-route-2026-08-09/regression-request-capture/`
- 清理证据：`真机证据/concept-dual-route-2026-08-09/runtime-concept-dual-route-cleanup.json`

唯一测试卡及绑定世界书均命名为 `《诡异药剂师》v0.5-概念双路验收-20260809-A`。证据落盘后，两者已精确删除；清理断言为卡不存在、世界书不存在、同名世界书数量为 0、本地临时卡和临时世界书文件不存在。随后又通过酒馆只读角色卡列表与世界书列表确认该名称不存在，未删除正式 `《诡异药剂师》v0.5`。

## 发布状态

AGENT B 已完成当前 SHA-256 的离线与真机验收。工程仍标记为 `candidate`，因为是否发布由所有者确认，不由测试代理自动改变。
