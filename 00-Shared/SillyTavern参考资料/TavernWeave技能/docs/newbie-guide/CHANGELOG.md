# 奶人教程更新日志

## 1.3.0 - 2026-08-18

- 功能地图同步为 20 个 Skill，新增“照镜子”成长核验入口，并把完整 AFV 前端设计/动效资料库标为本次同批更新。
- 更新日志新增 `V1.3.0 · 照镜子与完整资料库`，只说明用户入口、可见能力和隐私边界；最新版默认展开，V1.2.0 顺延为折叠历史。
- Library 入口说明更新为 462 设计、194 动效、86 概念、1,609 条蒸馏账本与 89 个本地沙盘；候选仍固定为 `proposed`，不会自动变成采用或验收。
- 安装核验同步为 `20/20`，教程版本升至 `1.3.0`，缓存标识升至 `v34`。

## 1.2.0 - 2026-08-17

- 新增开篇 `TavernWeave 功能地图`：按项目规划与脑暴、角色卡与变量、前端与美术、调试与优化、资料库与发布五套系统解释 TW 的能力和直接使用口令，不要求新手先背 19 个 Skill 名称。
- 新增绿色文字徽标 `NEW · V1.2.0`，同时出现在目录与正文能力卡；颜色不单独承担含义，亮色、暗色与窄屏共用同一语义。
- 新增末尾独立的 `更新日志`，不占用第五章编号；集中展示 `V1.2.0 / V1.1.0 / V1.0.0` 的用户可见功能、入口和操作方式，不把兼容性、验证结果、待验证项或 Git 流水写进公开教程。
- 大版本日志采用最新版默认展开、旧版折叠的结构；点击目录中的旧版本时会自动展开对应日志。原 Hero 移入开篇分片，原 `00–45` 教学编号保持不变，页脚版本由残留的 `v0.5.7` 修正为 `v1.2.0`。
- 第五章升级为 Host Front Door → Soul 单席/三席 → 脑暴编排 → Library → 19 个 Skill 的五层入口。
- 新增 `05.5 脑暴与防膨胀蓝图`：用可复制口令演示 Soul 三席联席、每轮 2～4 个决策、方向讨论上限、Core Spine / First Playable / Growth Tracks / Parking Lot、三档蓝图体量，以及任意活动步骤的持久蓝图零预算与真实问题临时细分/回归合同。
- 明确同层或独立前端不合适时应劝退；驾驶员强行覆盖也必须保留 `driverOverride`、原型和真实宿主门。
- 安装核验同步为 `19/19`，并单独检查 `orchestrate-project-blueprint`；教程版本保持 `1.2.0`，缓存标识升至 `v33`。
- 新增脑暴、开篇能力地图、大版本日志、绿色标记、折叠展开和完整锚点顺序回归测试；真页面桌面/窄屏与驾驶员体验验收仍按独立门执行。

## 1.1.1 - 2026-08-17

- 修复正文实际从 `05.4` 排到 `00` 之前造成的目录错位；`05.1` 至 `05.4` 现与第五章相邻，目录、DOM 与阅读顺序完全对齐。
- 目录点击改为显式目标定位，移动端会先关闭目录、恢复页面位置再滚到目标；高亮依照真实正文顺序判断，滚动与缩放使用动画帧节流。当前项会自动滚入左侧目录可视区，顶部总数由页面实际条目生成，不再硬编码 `/49`。
- 新增独立的 `03.3 Codex / Claude Code 雷达`：恢复 Codex Radar 推荐，明确 Claude Code Radar 于 `2026-08-17` 暂时关闭，并增加 Electricity Bench 作为跨 Agent 补充观察站；原模型体检顺延为 `03.4`。
- 新增目录顺序、锚点、雷达链接和动态导航回归测试；教程版本升至 `1.1.1`，缓存标识升至 `v31`。4 项专项回归与整仓验证通过；1440px/390px 真页面下的目录点击、直接锚点、上下滚动跟随、抽屉回显、横向溢出和控制台检查均通过，驾驶员体验验收已于 2026-08-17 通过。

## 1.1.0 - 2026-08-17

- 将 v1 入口更新为 Host Front Door → 三种 Soul 模式 → Library → 18 个工程 Skill，并解释全局入口不等于跨任务人格状态持久化。
- 新增灵魂杀手与强尼·银手非官方前端审查彩蛋：允许嘴臭但必须有证据、修法和复验，安全词优先，禁止身份羞辱、官方/演员冒充与原作台词或素材复制。
- 安装流程增加 Host Front Door 的明文推荐、diff 与受控区块边界；继续区分 18/18、前门 current、宿主发现和真实 ST。
- 增加 DSH Developer Preview / TW Lite 提示，明确官方 minimal 无 Skill loader，当前没有真实运行或智力 A/B 证据。
- 教程版本升至 `1.1.0`，缓存标识升至 `v26`；1280px/390px 自动浏览器验收、目录交互、复制、主题、锚点和控制台检查通过，驾驶员体验验收已于 2026-08-17 通过。

## 1.0.1 - 2026-08-16

- 新增 `05.4 安装不是数文件夹`，解释仓库源码、实际安装目录和宿主发现是三道不同的门。
- 安装口令要求按仓库完整清单创建新增目录，不得只覆盖已有 Skill。
- 加入 18/18、Soul、Library 与离线挑选页安装回执，以及安装后新建任务的独立发现门。
- 更新教程版本至 `1.0.1`，缓存标识至 `v25`；桌面、390px 窄屏与驾驶员验收通过。

## 1.0.0 - 2026-08-16

- 新增 TavernWeave v1 三层入口：Portable Soul → Routed Library → 18 个工程 Skill。
- 新增阿瞳 / MTTT.sir 激活、互切、退出、身份和持久性边界说明。
- 新增 A0、ST 指南、设计/动效、来源 Wiki、离线挑选页与 `proposed` 候选语义。
- 明确 A1、归档指南、私有 RAG、本地证据与 AFV 无关资料不进入公开分发。
- 桌面与 390px 窄屏浏览器检查及驾驶员验收通过；发布仍由独立门控制。

## 0.5.7 - 2026-08-06

### 子章节分隔与目录跟随

- 为 `02.1`、`03.1`、`03.2`、`03.3` 增加带编号的正文分隔线，长章节不再连成一整堵文字墙。
- 目录监听从只跟踪外层章节，改为跟踪所有目录锚点；点击子章节会立即选中，继续滚动时高亮会跟随当前主章节或子章节。
- 加强子章节选中样式，并在滚动进入其他章时自动展开对应目录组。
- 手机目录打开时锁定并恢复正文滚动位置，避免抽屉出现后当前子章节高亮退回上一节。
- 更新教程版本至 `0.5.7`，缓存标识至 `v23`。

## 0.5.6 - 2026-08-06

### 开工前模型体检

- 新增 `03.3 开工前模型体检`：用于开工、续费或购买模型厂商官方套餐／API 前查看近期能力、速度、额度和稳定性。
- 明确选购顺序为公开雷达观察、固定任务验收、模型厂商官方渠道优先；第三方接口检测下沉为补充证据，不进入选购主线。
- 收录 Codex 雷达、分布式雷达、Artificial Analysis、LiveBench、SuperCLUE 与 OpenCompass，并解释公开榜不能替代用户自己的真实项目验收。
- 收录 Claude 与国模接口检测站，区分签名验真、行为指纹、来源判断和能力跑测，补充临时限额 Key 与检测后吊销要求。
- 更新教程版本至 `0.5.6`，缓存标识至 `v19`，目录新增可直接跳转的 `03.3` 子项。

## 0.5.5 - 2026-08-06

### 整篇去八股

- 重写第一至四章的标题、章节引导和关键提示框：删掉课本腔、汇报腔和反复出现的同义提醒，改成更直接、更好记的人话。
- 保留代码、命令、路径、API 名称、安全边界与验收等级，不用“生动”换走技术准确性。
- 统一目录文案，让侧栏与正文使用同一种口气，同时控制长度，避免把长标题硬塞进导航。
- 收紧 Agent、Zod、CoT、脚本、正则、真实状态和运行时调试的核心分工，用“施工单”“记账”“算账”“案发现场”等具体比喻替换空泛概念。
- 更新教程版本至 `0.5.5`，缓存标识至 `v18`。

## 0.5.4 - 2026-08-06

### 国内环境付费避雷

- 新增 `03.2 国内环境付费避雷`，把 Cursor 编辑器与 Cursor 高价套餐分开评价。
- 根据当前国内环境实测，明确不推荐购买 Cursor Pro+，强烈不推荐购买 Cursor Ultra：Fable／Opus 无法形成可用工作流时，再大的额度也只是大号空桶。
- 补充 Kiro Claude 的实际验货结论：智力、可靠性与 Agent 连续执行能力均未达到正式项目要求。
- 对照 Kiro 官方资料说明 Auto 的混合模型路由，同时保留证据边界，不把无法从外部证明的后台替换写成既定事实。
- 将 OpenCode 加入 Agent 选型器和工具对比表，作为国内环境中可明确选择供应商、模型与 Base URL 的优先方案。
- 更新缓存标识至 `v17`，目录新增可直接跳转的 `03.2` 子项。

## 0.5.3 - 2026-07-27

### Agent 与套餐

- 在第一章 02 的 Agent 选型后新增 ChatGPT 官方移动端订阅说明。
- 分开说明 ChatGPT 注册、首次 API Key 手机号验证、服务支持地区和移动端购买，不把它们合并成“绕过手机号”流程。
- 补充 Apple App Store、Google Play、礼品卡地区限制与账号安全边界。
- 将 `jieoupen/Google-Play-Store` 限定为非官方的安卓设备兼容性参考，不把第三方安装器或虚拟空间当成购买方案。

### 第三方模型

- 第一章 03 调整为“模型选择、第三方接入与双 Agent 副驾驶”。
- 新增 CC Switch 与 Codex++ 的定位、配置步骤、选择建议和验收要求。
- 明确 Responses、Chat Completions、本地路由、工具调用和桌面端兼容性边界。
- 套餐购买与第三方模型配置分别位于 02、03，不互相混写。
- 章节目录新增 `02.1 ChatGPT 套餐购买` 与 `03.1 第三方模型接入` 子项，可直接跳转到对应教程。

## 0.5.2 - 2026-07-26

在线阅读：[TavernWeave · Vibe Code 制卡入门](https://liarmttt.github.io/TavernWeave/)

这次更新把教程从“会让 Agent 修改东西”，继续推进到“看懂 Agent 怎么工作，并能根据真实运行证据指挥它 Debug”。

### 新增与补全

- 第一至三章划为入门篇，第四章开始进入进阶篇。
- 补全第一章 02：用零基础能理解的方式解释 Agent 是什么、接到任务后怎样查看现场、使用工具、检查结果和继续修正。
- 加入新手指挥模板、含糊需求与可执行需求对比、实际反馈模板，以及用户和 Agent 的职责边界。
- 强调知识越全面，指挥 AI 的效率越高；暂时讲不清时，可以向 AI 提供可靠样本，让它先弄明白，再用你能听懂的方式教回来。
- 新增第四章“监听真实酒馆实例”，介绍真实 SillyTavern、调试 Probe、本机 Mock 接收端和 Agent 事件分析组成的调试链。
- 增加 Agent Mock 与 VS Code 的对比：前者适合观察长时间事件顺序和生命周期，后者适合检查断点、调用栈与局部变量。
- 加入联合调试路线：先用事件流缩小范围，再用断点检查精确状态，最后回到真实酒馆完成正式验收。

### 阅读体验

- PC 页面扩大正文空间，减少无意义装饰，把信息密度和可读性放在前面。
- 左侧目录改为按章节折叠，并明确标出“第一至三章入门、第四章起进阶”。
- 顶部说明调整为“Vibe Code 制卡入门”，避免被误解为泛用 Vibe Code 教程。
- 手机端继续保留抽屉目录和纵向阅读布局。

### 阅读建议

- 第一次接触 Agent：从第一章 00、01、02 开始。
- 正在制作变量卡：阅读第二章的 Zod、CoT 与计算脚本。
- 正在制作状态栏、按钮或交互：阅读第三章的脚本、正则、UI 和生命周期。
- 功能只在真实酒馆中出问题：直接进入第四章，从运行时证据和联合调试开始。

### 需要记住

- Agent 的完成声明不能代替实际结果。
- 文件存在、页面出现和构建成功，都不能单独证明功能正确。
- 真实 SillyTavern 仍然是角色卡功能的最终验收环境。
- 第四章的接收器只是讲清原理的最小示例，不是所有项目都应该照抄的标准答案。
- 应先调查自己的构建方式、运行边界、可用事件、故障类型和敏感数据，再设计合理的 Debug 机制。
