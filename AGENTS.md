# 《诡异药剂师》项目常驻规则 (A0 Harness)

## 当前维护入口（2026-09-13）

**当前运行卡体**为 **v0.18（0.18.0）**（`角色卡本体/诡异药剂师_MVU_v0.18`，止于 **E492** / S0—S66）。v0.18设定工作区（E435—E492 / 原文1768—1926）已整卡集成，只读。v0.17（E434）与 v0.16（E396）为冻结基线，勿回改。先读[版本与工作区索引](版本与工作区索引.md)及对应目录 `AGENTS.md`。真实 SillyTavern 实测按作者要求暂缓；不自动导入、创建聊天或运行宿主测试，host_acceptance 保持 pending。

## 全局最高工程原则
1. **最简可维护**：优先最少文件变更、最少新增抽象、最少依赖；禁止为不存在的问题提前设计复杂架构。
2. **拒绝补丁堆叠**：若已有架构不适合，优先重构核心路径，不继续堆叠兼容补丁。
3. **玩家主权绝对独占**：林恩对白、动作、心理与记忆始终由玩家独占，AI 绝不越权决策或代写内心。

## 开工三格检查 (动手前必须复述纠偏)
每次任务开工前，先用三句话对齐并等待确认：
- **目标**：本次要做成什么（一句话可验收，顺带说明不做甚么）。
- **红线**：本次绝对不能碰什么（如禁止手改 dist、禁止元数据泄底）。
- **验收**：怎么算做完（可执行的测试命令或真实环境证据）。

## 角色卡创作铁律
- **色色机制三等价**：以M=快感冲击（媚药）、性行为=解咒途径、恶堕=性调教。
- **色色直白化**：成立则直接写动作、身体反应、过程与结果；不占位、不叠甲、不用回避话术或非性替代糊弄。幼态外形固定非性。
- **禁元数据词汇**：正文严禁出现“第X章、阶段几、小总结”等词，来源一律用 E 编号。
- **改动闭环**：源码修改后必须运行 `npm run check` 离线校验；离线通过 ≠ 真实交付。
