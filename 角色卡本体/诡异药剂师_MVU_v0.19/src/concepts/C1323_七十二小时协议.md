# 概念·契约／钢印·七十二小时协议（事件["E470","E471","E472","E473","E474","E476"]）

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E470.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E471.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E472.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E473.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E474.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E476.状态", { defaults: "未触发" }))) { _%>
# C1323·七十二小时协议
（幽灵重映射：原误引C1056→本号）

- 类别：契约／钢印
- 未知项：原文未给出可独立核验的闭环边界，缺证处保持开放。
- 事实适用：72h对等协议总条；协议一伤害解体缩小、协议二限制解除作交易筹码分事件展开。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E470.状态", { defaults: "未触发" }))) { _%>
## E470
- 定义：共同起草的72小时钢印协议，植入光宴底层根数据。
- 来源：E470
- 机制：①时限内光宴不得伤害对方及其阵营盟友，或产生类似想法，否则立即解体。②时限内若对方伤害光宴或产生类似想法，则条款一失效。③试图解除契约＝违条款一。念头亦触发；调戏≠伤害故协议二可不触发。
- 限制与代价：对等设计双方满意才植入；威胁终止合作可逼签。
- 禁止外推：不把协议写成永久主奴契约。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E472.状态", { defaults: "未触发" }))) { _%>
## E472·协议一
- 定义：协议一＝伤害触发解体缩小；可连裂为膝高群体直至近三位数／钉死98份意志。
- 来源：E472
- 机制：挥拳／杀意触协议一→解体为更小光宴；再打可再裂。只对衣服动武可不触发（E476对照）。
- 禁止外推：不把幼态／袖珍外形写成性对象。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E474.状态", { defaults: "未触发" }))) { _%>
## E474·协议二筹码
- 定义：协议二限制解除可作为交易筹码（帮制服对方＋再签协议、一定程度听指令）。
- 来源：E474
- 机制：竞价中提出；选定者可被对手背刺吞噬，承诺未兑现。
- 禁止外推：不把协议二解除写成已永久生效。
<%_ } _%>

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E471.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E473.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E476.状态", { defaults: "未触发" }))) { _%>
## 续存约束
- 定义：协议在E471–E476期间持续约束伤害／杀意；出拳可被拦截；诅咒之城话题亦可触杀意预警。
- 来源：E471、E473、E476
- 禁止外推：不把「伤害」语义钻空（推倒／愉悦是否算伤害）写成已裁定条款。
<%_ } _%>
<%_ } _%>
