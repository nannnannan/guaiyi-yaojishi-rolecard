# 概念·纳米基建／细胞反控·纳米风暴建厂与巨像细胞反控（事件["E460","E503","E504","E530","E534","E535"]）


<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E503.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E530.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E534.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E535.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E503、E504、E530、E534、E535完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E503.状态", { defaults: "未触发" }))) { _%>
  E503：纳米风暴建厂服务机械七神／反叛产能。 纳米级基建可快速铺厂；与巨像细胞反控议题尚处同阶段铺垫。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" }))) { _%>
  E504：巨构表层与试验体可嵌纳米修复／拟态。 试验失败时纳米修复赶不上侵蚀。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E530.状态", { defaults: "未触发" }))) { _%>
  E530：私藏品试探中，纳米拟态皮肤介质可作伪装／感官接口。 拟态用于身份与触感层，不自动提供神王级防御。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E534.状态", { defaults: "未触发" }))) { _%>
  E534：巨像细胞／分泌残留感应：名义主人权限；另一权限者可屏蔽。 血肉气息／分泌残留可被感应追踪；名义主人有权限，另一权限者可屏蔽侦测。战损爆炸重建与伪当机可配合纳米修复表演。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E535.状态", { defaults: "未触发" }))) { _%>
  E535：切磋中机械巨龙等形态依赖纳米／原子机械重组。 半小时切磋展示重组耐力；远程嘱勿泄身份。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E503.状态", { defaults: "未触发" }))) { _%>
  限制与代价：风暴可误伤友军设施。
  未知项：建厂主权。
  禁止外推：禁止写成纳米已覆盖全地狱。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" }))) { _%>
  限制与代价：修复带宽有限。
  未知项：拟态皮肤介质完整规格。
  禁止外推：禁止写成纳米无敌。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E530.状态", { defaults: "未触发" }))) { _%>
  限制与代价：被高位识破风险。
  未知项：介质维持时间。
  禁止外推：禁止写成拟态不可被看穿。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E534.状态", { defaults: "未触发" }))) { _%>
  限制与代价：权限对撞时结果依现场更高权。
  未知项：权限如何授予。
  禁止外推：禁止写成感应绝对或绝对不可感应。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E535.状态", { defaults: "未触发" }))) { _%>
  限制与代价：重组耗材与热量。
  未知项：细胞反控对巨像本体的百分比。
  禁止外推：禁止写成已完全控制巨像全身细胞。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：仅保留已经具备门槛的既往事实。
- 当前状态：限制与代价：进一步范围尚未确认。 未知项：进一步范围尚未确认。 禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
