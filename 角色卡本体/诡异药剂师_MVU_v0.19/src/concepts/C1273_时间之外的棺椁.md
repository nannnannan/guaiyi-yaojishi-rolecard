# 概念·物品·时间之外的棺椁（事件["E390","E391","E392","E404","E408","E495","E497","E504","E505"]）


<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E495.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E497.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E505.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E495、E497、E504、E505完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E495.状态", { defaults: "未触发" }))) { _%>
  E495：无序拖带／覆体防线容器：棺椁可释放无序成领域迫以太断肢抽离。 棺椁释放无序顷刻成绵延几十米漆黑满月领域；可迫以太切断小半边身躯。无序拖带把深渊之力带向地下飞腾。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E497.状态", { defaults: "未触发" }))) { _%>
  E497：棺椁可挡一击但不可躲入（防夺）；强行扭入空间背面虚无会被撕出。 三连杀招下棺椁是防线容器不是避难所；躲入有被夺风险。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" }))) { _%>
  E504：真以太切片／试验场中，棺椁仍作时间之外的收容参照，与银色圆球时间停滞立场对照。 恒星矩阵试验不替代棺椁的时间外属性。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E505.状态", { defaults: "未触发" }))) { _%>
  E505：棺椁卡缝破界：对面＝空间乱流／夹层虚空。 破界不是开门回主宇宙，而是卡进乱流／夹层；用于逃脱或换场，生存条件恶劣。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E495.状态", { defaults: "未触发" }))) { _%>
  限制与代价：领域误伤友军风险；棺椁非无限无序源。
  未知项：领域最大直径。
  禁止外推：禁止写成棺椁已吞噬以太本体。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E497.状态", { defaults: "未触发" }))) { _%>
  限制与代价：挡得住次数有限。
  未知项：防夺机制细节。
  禁止外推：禁止写成躲进棺椁即绝对安全。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E504.状态", { defaults: "未触发" }))) { _%>
  限制与代价：两种时间手段不可混用为同一件。
  未知项：切片容器与棺椁是否同构。
  禁止外推：禁止合并成单一神器百科。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E505.状态", { defaults: "未触发" }))) { _%>
  限制与代价：夹层不可长期停留。
  未知项：卡缝可重复次数。
  禁止外推：禁止写成破界即回家。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：仅保留已经具备门槛的既往事实。
- 当前状态：限制与代价：进一步范围尚未确认。 未知项：进一步范围尚未确认。 禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
