# 概念·权限／暗号·智械权限遗产与涂鸦改笔（事件["E477","E493","E494","E495","E496","E514"]）


<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E493.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E494.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E495.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E496.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E514.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E493、E494、E495、E496、E514完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E493.状态", { defaults: "未触发" }))) { _%>
  E493：学者吞噬与革命启动把智械权限遗产推入争夺。 遗产不再只是文本权限，而是飞升拼图碎片。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E494.状态", { defaults: "未触发" }))) { _%>
  E494：龙首分裂智械女儿与田园火种；上传芯片抛投；灭绝者截芯与绝对理智认同风险。 最后百分之一＝银幻万机遗产。龙首求留过去火种；将军曾答应保留存在但削权。芯片被截则遗产易主。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E495.状态", { defaults: "未触发" }))) { _%>
  E495：将军欲独揽决策，否定七神共治式权限分配。 权限遗产政治化：谁决策谁飞升。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E496.状态", { defaults: "未触发" }))) { _%>
  E496：七神权限齐全≠完成；缺银幻则拼图缺口仍在。 权限清单与进度条解耦：集齐权限只是必要条件之一。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E514.状态", { defaults: "未触发" }))) { _%>
  E514：银幻＝原子浪潮剩余最后1%被公开计价。 遗产从暗线变成全场目标。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E493.状态", { defaults: "未触发" }))) { _%>
  限制与代价：被吞噬者权限可被夺。
  未知项：涂鸦改笔本场是否动用。
  禁止外推：禁止写成遗产已集齐。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E494.状态", { defaults: "未触发" }))) { _%>
  限制与代价：截芯＝权限可能落入灭绝者。
  未知项：火种与遗产的集合关系。
  禁止外推：禁止写成芯片已安全交付玩家。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E495.状态", { defaults: "未触发" }))) { _%>
  限制与代价：独揽失败即被以太夺控。
  未知项：涂鸦改笔能否改飞升契约。
  禁止外推：禁止写成改笔已改写革命成功。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E496.状态", { defaults: "未触发" }))) { _%>
  限制与代价：强行凑权限可引发内斗。
  未知项：缺口可否伪造。
  禁止外推：禁止写成权限已齐且革命完成。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E514.状态", { defaults: "未触发" }))) { _%>
  限制与代价：计价招来猎杀。
  未知项：改笔能否改「1%」定义。
  禁止外推：禁止写成1%已交出或已销毁。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：仅保留已经具备门槛的既往事实。
- 当前状态：限制与代价：进一步范围尚未确认。 未知项：进一步范围尚未确认。 禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
