# 概念·灵魂残片与幕后知识·渡鸦遗体与世界稳定（事件["E362","E512","E513","E514","E522","E524","E525","E527","E539"]）


<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E512.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E513.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E514.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E522.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E524.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E525.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E527.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E539.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E512、E513、E514、E522、E524、E525、E527、E539完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E512.状态", { defaults: "未触发" }))) { _%>
  E512：渡鸦尸体为链接成功的外部锚。 最大牢笼预谋中，渡鸦尸体提供外部锚点，使心灵之海映射／送入链接可成立。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E513.状态", { defaults: "未触发" }))) { _%>
  E513：渡鸦尸体感应与「备份归位」媒介。 登山／心灵之海路径上，尸体提供感应与备份归位媒介，服务左左／残魂线。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E514.状态", { defaults: "未触发" }))) { _%>
  E514：渡鸦刺胸熔铸与本体／残魂合流。 刺胸熔铸把渡鸦遗体权能导入合流过程，服务矩阵封印与精神世界作战。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E522.状态", { defaults: "未触发" }))) { _%>
  E522：渡鸦尸体地理近距＝主动顺访动机。 罪孽城行动中，尸体位置接近成为顺访理由之一。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E524.状态", { defaults: "未触发" }))) { _%>
  E524：渡鸦节点；第三只渡鸦尸体在深渊下方（未见）。 咒文巨墙／结界／漏斗／深渊；无狱卒默认许可解读。第三只尸体只被指向未见。面具温热与时间流动涟漪显示遗体／节点仍在维持某种时间相关稳定。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E525.状态", { defaults: "未触发" }))) { _%>
  E525：渡鸦盒嘲讽赠还；面具＋甲胄外限带两物回过去；渡鸦尸躯作时间容器的同族逻辑再被触及。 时间战争赌约中，渡鸦相关遗物限制可带回过去的物品数量与性质。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E527.状态", { defaults: "未触发" }))) { _%>
  E527：二十一万年前墓地／丰碑场，渡鸦遗体系稳定历史节点的背景参照仍在。 半封裂隙卡战争进行中；遗体稳定世界的功能以「节点未崩」侧写，不新造第二具。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E539.状态", { defaults: "未触发" }))) { _%>
  E539：第二只渡鸦误导线反衬遗体／神话的多尸命题。 「从来不止一只」与误导线并读时，遗体条目增加「多尸并存」索引，不自动鉴定每具。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E512.状态", { defaults: "未触发" }))) { _%>
  限制与代价：无尸锚则链接窗口不稳定。
  未知项：锚与世界稳定度的定量关系。
  禁止外推：禁止写成尸体已复活。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E513.状态", { defaults: "未触发" }))) { _%>
  限制与代价：媒介≠完整渡鸦意识归来。
  未知项：备份内容清单。
  禁止外推：禁止写成渡鸦已完全复活。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E514.状态", { defaults: "未触发" }))) { _%>
  限制与代价：熔铸消耗遗体完整性。
  未知项：合流后渡鸦人格残留。
  禁止外推：禁止写成熔铸无代价。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E522.状态", { defaults: "未触发" }))) { _%>
  限制与代价：近距不等于安全。
  未知项：现场守卫情况。
  禁止外推：禁止写成尸体已转移出地狱。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E524.状态", { defaults: "未触发" }))) { _%>
  限制与代价：未见＝不可当已回收战利品。
  未知项：第三只与世界稳定的具体贡献。
  禁止外推：禁止写成第三只已打捞上岸。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E525.状态", { defaults: "未触发" }))) { _%>
  限制与代价：容器有外限。
  未知项：尸躯时间容器与盒／面具的关系。
  禁止外推：禁止写成可无限携带。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E527.状态", { defaults: "未触发" }))) { _%>
  限制与代价：历史场不等于可随意取用遗体权能。
  未知项：丰碑与渡鸦遗体的制度关系。
  禁止外推：禁止写成遗体已在远古被销毁。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E539.状态", { defaults: "未触发" }))) { _%>
  限制与代价：误导可污染鉴定。
  未知项：各具遗体的真伪。
  禁止外推：禁止把误导当成遗体鉴定结论。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
  未知项：进一步范围尚未确认。
  禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：仅保留已经具备门槛的既往事实。
- 当前状态：限制与代价：进一步范围尚未确认。 未知项：进一步范围尚未确认。 禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
