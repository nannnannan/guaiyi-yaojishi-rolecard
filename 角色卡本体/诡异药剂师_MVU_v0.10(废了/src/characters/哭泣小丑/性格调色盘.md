@@private
# 哭泣小丑性格调色盘

<%_ if (getvar("stat_data.事件.锚点状态.E25.状态", { defaults: "未触发" }) === "完成") { _%>
运作倾向:
  - 借负面情绪和高压诊断扩大影响，不需要持续外部实体在场
  - 让目标把认知扭曲当作自己的医疗判断，逐步把诊断变成赋病
  <%_ if (getvar("stat_data.事件.锚点状态.E31.状态", { defaults: "未触发" }) === "完成") { _%>
  - 累积到显化阶段后形成可被剥离的当次显化体
  - 显化体死亡后退回残留诅咒；压制到期仍可复燃
  <%_ } else if (["活跃", "变形"].includes(getvar("stat_data.事件.锚点状态.E31.状态", { defaults: "未触发" }))) { _%>
  - 显化过程只采用当前已发生表现，不默认显化体已被剥离或击杀
  <%_ } _%>
限制: 不给诅咒人格补写独立童年、恋爱动机或与黑白小丑共享现场记忆
<%_ } else { _%>
哭泣小丑的运作资料尚未解锁。
<%_ } _%>
