@@private
# 哭泣小丑基础信息

<%_ if (getvar("stat_data.事件.锚点状态.E25.状态", { defaults: "未触发" }) === "完成") { _%>
诅咒档案:
  名称: 哭泣小丑
  类型: 林恩体内的诅咒人格与可分离显化体
  来源: E25未完成的心与脑之汤
  作用: 放大负面情绪，扭曲认知，使诊断逐步变成向目标赋予疾病
  <%_ if (getvar("stat_data.事件.锚点状态.E31.状态", { defaults: "未触发" }) === "完成") { _%>
  E31结果: 当次显化体被剥离并击杀；诅咒根因残留
  <%_ } else if (getvar("stat_data.事件.锚点状态.E31.状态", { defaults: "未触发" }) === "变形") { _%>
  E31变形约束: 只采用事件实际记录的替代结果，不默认显化体已被剥离或击杀
  <%_ } _%>
  <%_ if (getvar("stat_data.事件.锚点状态.E34.状态", { defaults: "未触发" }) === "完成") { _%>
  E34结果: 天使血仅压制约十五日，后续会再次爆发
  <%_ } else if (getvar("stat_data.事件.锚点状态.E34.状态", { defaults: "未触发" }) === "变形") { _%>
  E34变形约束: 只采用事件实际记录的替代结果，不默认获得十五日压制
  <%_ } _%>
  区分: 黑白小丑是独立本体，不能与哭泣小丑合并
  边界: 固定非恋爱，吸引与恶堕固定为0
<%_ } else { _%>
哭泣小丑诅咒尚未进入林恩体内。
<%_ } _%>
