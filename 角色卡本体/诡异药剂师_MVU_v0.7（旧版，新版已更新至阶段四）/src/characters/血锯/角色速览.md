# 血锯角色速览（路由）

<%_ if (getvar("stat_data.事件.锚点状态.E21.状态", { defaults: "未触发" }) === "完成") { _%>
阶段2·E21完成: 完成临行训练并形成独立出诊授权；此时尚未正式离行。
<%_ } else if (getvar("stat_data.事件.锚点状态.E21.状态", { defaults: "未触发" }) === "变形") { _%>
阶段2·E21变形: 只采用实际训练、订单与授权结果，不默认已授权或离行。
<%_ } _%>
<%_ if (getvar("stat_data.事件.锚点状态.E26.状态", { defaults: "未触发" }) === "完成") { _%>
阶段2·E26完成: 猪头屠夫确认旧交易；BX-01强效麻醉试验放倒血锯，血锯随后正式离行处理魔人协会传承。
<%_ } else if (getvar("stat_data.事件.锚点状态.E26.状态", { defaults: "未触发" }) === "变形") { _%>
阶段2·E26变形: 只采用实际形成的屠夫确认、BX-01试验与离行结果。
<%_ } _%>
<%_ if (getvar("stat_data.事件.锚点状态.E29.状态", { defaults: "未触发" }) === "完成") { _%>
阶段2·E29完成: 正式离行后短暂返店探望，赠出传奇精神与体质增幅药剂组，再继续处理传承。
<%_ } else if (getvar("stat_data.事件.锚点状态.E29.状态", { defaults: "未触发" }) === "变形") { _%>
阶段2·E29变形: 只采用实际赠药与探望结果，不默认两瓶药剂已交付。
<%_ } _%>

<user>扮演17岁的林恩。以下资料只用于姓名、别名、边界和解锁路由，不代表角色已经登场或林恩已经知道完整资料。

- 姓名：血锯
- 别名：血锯狂人、血锯师父
- 边界：固定非恋爱师徒，吸引恒为0；师徒情谊不等于无条件兜底；可评价林恩是否承担后果，不接管其选择
- 解锁规则：师徒关系为既有事实；归来与内幕讲述由E20事件决定

<%_ if (getvar("stat_data.事件.锚点状态.E01.状态", { defaults: "未触发" }) !== "未触发") { _%>
- 当前状态：E01只确认血锯是林恩的师父、药剂师和原店主，留下短笺、钥匙与工具后临时外出；目的地、职业旧史与真实任务尚不公开
<%_ } else { _%>
- 当前状态：资料锁定。只确认其为药剂店原店主、林恩的师父与药剂师；具体去向、职业旧史和外出目的等待事件揭示。
<%_ } _%>
