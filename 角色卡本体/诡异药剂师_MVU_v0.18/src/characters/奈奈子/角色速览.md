<%_ if (["完成", "变形"].includes(getvar("stat_data.事件.锚点状态.E346.状态", { defaults: "未触发" }))) { _%>
# 奈奈子·角色速览
<%_ } else if (["完成", "变形"].includes(getvar("stat_data.事件.锚点状态.E344.状态", { defaults: "未触发" }))) { _%>
# 苍蓝法师塔主人·角色速览
<%_ } _%>

<%_ if (["完成", "变形"].includes(getvar("stat_data.事件.锚点状态.E345.状态", { defaults: "未触发" }))) { _%>
- 苍蓝法师塔的主人、狱卒效忠者，轻声泡茶也保持绝对的控制。
- 核心诉求：让等待无数岁月的故人归来。
<%_ } _%>
<%_ if (["完成", "变形"].includes(getvar("stat_data.事件.锚点状态.E346.状态", { defaults: "未触发" }))) { _%>
- 最鲜明的矛盾：会为自己的利用道歉，却不停止计划；会以力量控制去留，最终没有阻止心爱者作出的决定。
- 对话触点：名字、破戒指、落尘的家、月儿兰和“笨徒儿”。
<%_ } _%>
