# 概念·机制·界门与地狱锁链（事件["E278","E279","E321","E322","E323","E332","E333","E369","E392"]）

- 类别：机制
- 事实门槛：对应事件完成或变形且正文实际出现后，才引用其事实；事件中的参考经过不代替玩家选择。

- 既往定义：界门与地狱锁链是地狱侧镇压体系的一体两面。界门是【门】技能在地狱与外界的边界上撑开的通道：它并非永固结构，其展开与存续完全依存于开门时动用的能量——能量足够则界门稳定扩张，能量不继则界门收缩、濒临关闭；这一弱点在E279被血肉支配者识破并以此撞击界门。地狱锁链是地狱对越界者的镇压力：当强敌靠近或穿越界门时，地狱会从界门缝隙与深处爆射出漆黑锁链，缠绕目标、以诅咒黑线编织镇压——锁链的强度"按目标等级"匹配：面对的是神躯、古神级存在，就调动足以捆缚神躯的锁链与诅咒，将对方层层镇压、抽离其中寄居的旧日意志。锁链与诅咒是地狱"按需加压"的机制化表达：目标越强，地狱镇压的规格越高，直至将其拖入地狱深处。
- 既往来源：E278界门被撑开、黑暗世界群魔被镇压；E279锁链爆射捆缚双神躯、诅咒黑线编织、按目标等级镇压、界门不永固弱点爆发。
- 既往机制：①界门开：以开门能量撑开两界通道；②依存：界门存续与扩张绑定开门时能量，能量不继则收缩；③锁链爆射：强敌越界时从界门缝隙爆射漆黑锁链；④诅咒编织：诅咒黑线缠绕目标，镇压其力量与意志；⑤按级镇压：锁链规格按目标等级匹配（神躯对神级锁链）；⑥拖拽：将目标拖入地狱深处、排斥异己意志。
- 既往补充事实：锁链捆上克苏鲁尸躯时，尸身上浮现一张张又哭又笑的旧日面孔——每一张都被锁链一道一道镇压抽离，像地狱在给神躯"清场"。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E322.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E323.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E332.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E321、E322、E323、E332、E333、E369、E392完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" }))) { _%>
  E321：E321门展开至一半；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E322.状态", { defaults: "未触发" }))) { _%>
  E322：E322艾雯支援后坐标被碰撞扰乱；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E323.状态", { defaults: "未触发" }))) { _%>
  E323：界门可进行黑暗世界内部转移； 这次借母树根源力量，以黑夜城为坐标。 自缚天使黑锁链属于主母的诅咒，不能因外观相似便当成地狱镇压锁链。 E323战场侧残门完成吞入后才闭合。 内部开门比跨越内外两界消耗少，但仍大量抽取母树力量，使枝干枯萎。 艾雯在黑夜城侧升华供能； 巨刃碰撞后该侧死线缩为奇点，而战场侧门仍存，裂纹中暴露空间乱流，吸入树体并将其送往未知荒原。 E321至E323记录战争中界门受干扰、崩解和坠落。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E332.状态", { defaults: "未触发" }))) { _%>
  E332：E332记录艾雯抵达并评估迁移。 E332中林恩展开黑色死线，艾雯化为心灵巨人并精准供能，母树追加力量； 两大根源协助使界门逐步扩大稳定，黑夜城城郊提前开垦并撤人，母树根系穿门扎入指定土地，通道随迁移完成闭合。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  E333：界门是在地狱内部或世界边界撑开的空间通道，其尺寸和稳定性依赖持续能量、开门者控制和两端落点。 地狱锁链则会对越界的高位存在实施按级镇压。 E333证明界门能够在黑暗世界内部转移体量超过黑夜城的欲望母树，但必须由林恩开门，艾雯升格后提供稳定灵能，欲望母树也输出自身力量，并由黑夜城提前清空落地区域。 ①开门定位：建立两端坐标和黑色死线。 ②持续供能：能量填入通道，决定开口尺寸与持续时间。 ③高位稳压：艾雯以精确灵能避免力量泄露。 ④目标自供：被迁移的母树输出力量，减轻开门负荷。 ⑤分段通过：根系、树干与随行者依次穿越。 ⑥落点管理：目标端必须清空足够空间并组织撤离。 ⑦自然闭合：供能结束后界门收缩关闭。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E369.状态", { defaults: "未触发" }))) { _%>
  E369：E369中同阶同行者逐个穿越界门抵达黑堡外围，通道显现惊动守卫，跨界受地狱规则严格约束。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E392.状态", { defaults: "未触发" }))) { _%>
  E392：E392中在屠刀追击与险境撤离中，众人合力维持界门稳定闭合，成功脱离险境。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E392.状态", { defaults: "未触发" }))) { _%>
  限制与代价：界门开启伴随空间波动与守卫警觉，跨界受位面规则制约，需要力量持续维持。
  未知项：界门在遭遇高位空间崩塌时的应急闭合机制。
  禁止外推：不得把界门写成无声无息的任意门，通行必须符合剧情空间事实。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  限制与代价：根源级本体迁移消耗巨大，母树在此前消耗后只能提供较弱力量。艾雯短时升格必须控制在不让诅咒剧烈动乱的范围。外力干扰、坐标暴露、落点拥挤和通道提前收缩都可能导致灾难。黑暗世界内部传送消耗低于跨界，但不是无消耗。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E323.状态", { defaults: "未触发" }))) { _%>
  限制与代价：不能把母树这一受创状态下的供能量当作所有根源转移的固定价格；艾雯失去供能目标没有直接证明他受反噬重伤。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  未知项：界门最大尺寸、持续上限、单次可迁移质量、落点误差、锁链对内部传送是否介入、母树根系是否全部通过及后续能否再次整体迁移，均未确认。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E323.状态", { defaults: "未触发" }))) { _%>
  未知项：距离耗能曲线、错位后重校坐标的方法、通行存活率。
<%_ } else { _%>
  未知项：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  禁止外推：禁止补造空间折叠载具、战舰反应炉供能、永久门或无准备的大规模瞬移。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E323.状态", { defaults: "未触发" }))) { _%>
  禁止外推：黑夜城侧闭合不等于战场侧同时消失。
<%_ } else { _%>
  禁止外推：进一步范围尚未确认。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：仅保留已经具备门槛的既往事实。
- 当前状态：限制与代价：界门依赖能量，持续供能即持续暴露弱点；锁链镇压伴随反噬（血色雷霆、诅咒加深）；对根源级存在镇压会引发对方暴怒与疯狂（E279泰坦的"狂怒拖行"即反向利用）。 未知项：锁链机制的判定上限、界门能否被永固化、锁链与诅咒对根源级是否总有成效均未知。 禁止外推：林恩对白/动作/选择玩家主权独占；不违背原著。
<%_ } _%>
