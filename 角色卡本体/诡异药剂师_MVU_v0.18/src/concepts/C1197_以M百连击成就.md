# 概念·成就·以M百连击成就（事件["E328"]）

- 类别：成就
- 事实门槛：对应事件完成或变形且正文实际出现后，才引用其事实；事件中的参考经过不代替玩家选择。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E328完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  E328：以M百连击是林恩在与欲望母树的主导权争执中，于极短时间连续打出一百次以M之拳后小概率触发的特殊成就。 它证明以M的操作熟练度提高，并给出永久数值调整：出手速度加2%，所需疼痛值降低2%，道德减1，对雌性生物的克制度加1%。 这是系统对技术熟练的结算； 它只改变连击相关数值，不会自动压制诅咒或确定主仆关系。 E328母树用藤蔓、根系和巨石反击，林恩持续以以M打断，十分钟后系统结算百连击成就与200点基础经验，并列出四项永久变化。 ①触发方式：极短时间内连续以以M之拳命中一百次。 ②概率门槛：系统明确为小概率触发，达到次数不保证每次都结算。 ③速度增益：以M出手速度永久加2%。 ④成本下降：发动所需疼痛值降低2%。 ⑤行为代价：道德减1。 ⑥对象修正：对雌性生物的克制度加1%。 ⑦与诅咒分离：该成就只记录连击熟练度； 母树诅咒是否被压制，要看后续正文实际发生的解咒过程。 系统评价“这说明宿主对以M的使用变得更加的熟练了”，并把道德下降与技术加成同时列入永久结算。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  限制与代价：连击需要目标、时间窗口、疼痛成本与连续施放能力；速度提高不会消除疼痛，也不会保证控制根源。对雌性生物的克制度只是数值修正，仍受位格、防御、反击与现场状态影响。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  未知项：“极短时间”的精确秒数、连击中断判定、成就是否只能触发一次、克制度的计算公式、对非雌性目标是否有同类成就、道德数值的实际行为影响，均未确认。
<%_ } else { _%>
  未知项：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  禁止外推：成就只结算系统数值；攻击方式、是否继续接触与关系走向由玩家选择。
<%_ } else { _%>
  禁止外推：进一步范围尚未确认。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：尚未形成此事物的现场记录。
- 当前状态：名称仅供辨识，来源、能力与归属待实际出现后确认。
<%_ } _%>
