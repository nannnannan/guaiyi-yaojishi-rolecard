# 概念·事件·园丁任务（事件["E316","E318","E320","E321","E324","E328","E329","E332","E333"]）

- 类别：事件
- 事实门槛：对应事件完成或变形且正文实际出现后，才引用其事实；事件中的参考经过不代替玩家选择。

- 既往定义：园丁任务是 E316 收服欲望母树（C941）后触发的长期养护型任务：林恩成为母树的"园丁"，每月至少浇水施肥一次（短期目标），长期目标为努力使其结出果子。任务奖励随养护进度累积：体质点、血肉掌控度、驯化度、异常知识，小概率触发神性奖励。该任务将"征服古神"的宏大事件落地为持续性的日常责任——一艘以嘴炮收服根源的船，从此要按时给树浇水。
- 既往来源：E316 母树臣服后触发任务【园丁】：每月浇水施肥至少一次、长期结果子。
- 既往机制：①短期目标：每月浇水施肥一次；②长期目标：努力使其结出果子；③奖励池：体质点/血肉掌控度/驯化度/异常知识；④概率奖励：小概率神性；⑤频率约束：按月结算。
- 既往补充事实：母树对"园丁"的验收相当严格——林恩某月偷懒，隔天树干上多出一行刻痕："上次施肥：47天前。"他默默把水桶提了起来。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E318.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E320.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E324.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E329.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E332.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E318、E320、E321、E324、E328、E329、E332、E333完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E318.状态", { defaults: "未触发" }))) { _%>
  E318：E318自然之泉与抵押； E318进入核心水潭并建立养护通道；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E320.状态", { defaults: "未触发" }))) { _%>
  E320：E320的系统把持续养护与树苗成长联系，并警示过度养护可能造成依赖。 E320成长成就和养护反馈； E320在正文实际发生解咒过程后，系统把养护判定为施肥播种并结算园艺家相关奖励；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" }))) { _%>
  E321：母树养护与解咒关系对应既有园丁任务。 E321开门耗去大量复苏力量。 性行为是解咒途径； 以M是带破防性质的快感冲击，参与欲望合流，不能与解咒途径混为一种能力。 成长成就的概率触发、奖励和园艺家称号分别以系统提示确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E324.状态", { defaults: "未触发" }))) { _%>
  E324：E324自然之泉的属性、产地与维稳用途被鉴定；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  E328：园丁任务是收服欲望母树后形成的长期养护义务：短期至少按月“浇水施肥”，长期努力让树结果。 这里的园艺词汇同时指向实际养护、原作性行为解咒路径、以M快感冲击、驯化反馈和自然神性获取。 以M负责放大快感、诱发欲望与病变，不能单独完成解咒； 原作解咒路径实际发生后，系统才把该次养护记为诅咒压制，具体接触与推进由玩家选择。 任务不是一次完成后永久收服根源，而是一条需要持续照料、检查病变、约束根系与处理生态后果的责任线。 E328因母树过量动用力量、腐化部落和吞噬地下族群，园丁职责扩展为检查病变、核对根系、处理生态风险与持续监督。 ①周期义务：每月至少养护一次，具体结算日未给出。 ②以M边界：以M只放大快感、诱发欲望与病变，不能独立压制诅咒。 ③解咒路径：原作性行为解咒路径实际发生时，该次养护才会被记录为诅咒压制，未发生就不会生成该结算。 ④数值反馈：体质、血肉掌控、驯服度、异常知识与小概率自然神性只按正文实际结算登记。 ⑤园艺家联动：养育树木时提高反馈与奖励，具体依赖机制未明。 ⑥健康检查：需关注病变、泉水、力量使用与复发周期。 ⑦根系治理：母树加入黑夜城前，还需处理未经允许的腐化、吞噬和秘密教团风险。 ⑧完成开放：长期“结果子”仍未兑现，单次压制只完成一次养护。 E328中林恩把园丁职责说成“维护你的健康生长”； 母树在诅咒压不住时则明确要求“请……请行使园丁的职责”。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E329.状态", { defaults: "未触发" }))) { _%>
  E329：E329根系运输到千公里外末端；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E332.状态", { defaults: "未触发" }))) { _%>
  E332：E332迁移前树体更葱郁并开红花、两根源供能；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  E333：园丁任务在此次推进中增加了整体移植与新址相处的现实要求。 母树仍需压制诅咒、恢复力量，同时能够提供根系运输； 进入黑夜城不能自动结束长期养护。 E333落根和花粉异动。 以M的冲击与解咒途径分别成立。 迁移前林恩在树心完成两小时深度性行为解咒与欲望合流，以M快感冲击（媚药）激发母树欲望本源，使得母树神性苏醒、枝头盛开血色红花、树体充盈葱郁，达成本次解咒与园丁养护指标。 开门由林恩发动，艾雯与母树供能，接收端提前清场，母树才能落入城外预备土地。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  限制与代价：开花并非结果，花粉也可能被母树主动用于影响居民；养护和治理问题不会因移植而消失。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  限制与代价：养护会消耗双方体力并激发欲望；若只有以M快感冲击、没有进入原作解咒路径，病变可能加速而不会形成压制结果。园丁身份不自动扩展对母树身体、根系与信徒的处置范围，具体行动仍需事件事实与玩家选择。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" }))) { _%>
  限制与代价：养护后的恢复可被后续战争和界门供能大量消耗，解咒不等于恢复全盛。每月任务不意味着每次接触都必得同一奖励。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  未知项：果实用途、迁移后恢复速度、周期奖励和新址根系范围。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  未知项：任务失败条件、缺席多久触发惩罚、果实形态与用途、奖励上限、园艺家称号是否升级、母树离开地狱后周期是否改变，均未确认。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" }))) { _%>
  未知项：结果产物、成熟周期、重复奖励与依赖形成的确切阈值。
<%_ } else { _%>
  未知项：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E333.状态", { defaults: "未触发" }))) { _%>
  禁止外推：两根源供能不可替换成战舰引擎；开花不结算神性果实。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E328.状态", { defaults: "未触发" }))) { _%>
  禁止外推：禁止把“结果子”写成已发生；亲密行为的推进、暂停或拒绝由玩家选择，园丁任务只记录已发生的养护与系统结算。
<%_ } else if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E321.状态", { defaults: "未触发" }))) { _%>
  禁止外推：以M合流破防与解咒途径分别成立。
<%_ } else { _%>
  禁止外推：进一步范围尚未确认。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：仅保留已经具备门槛的既往事实。
- 当前状态：限制与代价：任务为长期义务，中断可能降低驯化度；母树处于诅咒91%未愈状态，浇水施肥可能需先中和诅咒；"结果子"的产物用途与后续影响未知。 未知项：果子的成熟周期与用途；神性奖励的具体表现；若长期缺席养护，母树契约（C1006）是否会反噬。 禁止外推：林恩的对白、动作与选择由玩家主权独占；禁止违背原著设定将其描写为无消耗或脱离因果律违规生效。
<%_ } _%>
