# 概念·地点·苍蓝世界法师塔与月儿兰花园（事件["E344","E345","E346","E347","E348"]）

- 类别：地点
- 事实门槛：对应事件完成或变形且正文实际出现后，才引用其事实；事件中的参考经过不代替玩家选择。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E344.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E345.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E346.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E347.状态", { defaults: "未触发" })) || ["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E348.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E344、E345、E346、E347、E348完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E344.状态", { defaults: "未触发" }))) { _%>
  E344：E344记录醒来、老人引路、狱卒庭、法师塔和破碎戒指；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E345.状态", { defaults: "未触发" }))) { _%>
  E345：E345记录塔内谈话、软禁与花园入口；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E346.状态", { defaults: "未触发" }))) { _%>
  E346：E346记录花园阵法、古老记忆和旧影消散；
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E348.状态", { defaults: "未触发" }))) { _%>
  E348：苍蓝世界法师塔是一座位于地狱相关特殊领域中的简陋高塔。 林恩醒来时只看到没有阳光和云彩的纯粹苍蓝、碧绿草地与鸟语花香。 塔外有鹅卵石路、篱笆、长椅和竹凳，桌上落着灰尘； 后院花园开满没有花香的洁白月儿兰。 这里既像居所，也像限制林恩活动的空间。 E347至E348由老人解释更深层领域。 ①高塔与苍蓝领域隔开外界冲击。 ②花园可承载无瞳纹路和紫罗兰记忆显影。 ③领域呈封闭回环，沿直线远行会回到原处，系统空间及既有能力无响应。 ④法师塔与黑海是否同一空间体系，需按老人说明与实际现场判断。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E348.状态", { defaults: "未触发" }))) { _%>
  限制与代价：位置、出口、创造者和领域边界未知；外宇黑暗仍能在苍蓝天空之外造成震荡。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E348.状态", { defaults: "未触发" }))) { _%>
  未知项：苍蓝世界的坐标、法师塔建造者、月儿兰来源、花园阵法响应条件、老人和塔主关系，均未确认。
<%_ } else { _%>
  未知项：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E348.状态", { defaults: "未触发" }))) { _%>
  禁止外推：不得把法师塔写成安全避难所或时间旅行地点，不得擅自补出塔内未出现的房间、道具和出口。
<%_ } else { _%>
  禁止外推：进一步范围尚未确认。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：尚未形成此事物的现场记录。
- 当前状态：名称仅供辨识，来源、能力与归属待实际出现后确认。
<%_ } _%>
