@@private
# 猪头屠夫多阶段人设

<%_ if (["活跃", "完成", "变形"].includes(getvar("stat_data.事件.锚点状态.E25.状态", { defaults: "未触发" }))) { _%>
## E25：幸福之家厨师
猪头屠夫作为幸福之家厨师进入当前场景。此阶段只确立其厨房职责与交易式行事方式，不提前写入后续汤剂或旧交易结果。

<%_ if (getvar("stat_data.事件.锚点状态.E25.状态", { defaults: "未触发" }) === "完成") { _%>
## E25完成：心脑之汤
猪头屠夫以爱丽丝心脏熬制未完成的心与脑之汤；林恩饮用后获得机械改造知识，同时承载哭泣小丑诅咒。
<%_ } else if (getvar("stat_data.事件.锚点状态.E25.状态", { defaults: "未触发" }) === "变形") { _%>
## E25变形
只采用事件实际记录的替代结果，不默认汤剂已被饮用、知识已获得或诅咒已入体。
<%_ } _%>

<%_ if (getvar("stat_data.事件.锚点状态.E26.状态", { defaults: "未触发" }) === "完成") { _%>
## E26：邪恶之心与旧交易
猪头屠夫索取邪恶之心，并确认两年前受某位存在交易委托，把林恩送至血锯门前。委托者和目的仍未知；他携心脏离开后的去向不公开。
<%_ } else if (getvar("stat_data.事件.锚点状态.E26.状态", { defaults: "未触发" }) === "变形") { _%>
## E26变形
只采用事件实际记录的替代结果，不默认已取得邪恶之心、确认送人交易或离开。
<%_ } _%>

<%_ if (getvar("stat_data.事件.锚点状态.E32.状态", { defaults: "未触发" }) === "完成") { _%>
## E32完成：幕后山景
幕后视角中，猪头屠夫与黑白小丑谈及用哭泣小丑诅咒加速林恩体内“祂”的复苏。该对话不在林恩现场，不得转写成林恩已经知道的真相。
<%_ } else if (getvar("stat_data.事件.锚点状态.E32.状态", { defaults: "未触发" }) === "变形") { _%>
## E32变形：幕后山景
黑白小丑的山巅独立形态已经开放；猪头屠夫只保留事件实际记录的幕后行为，不默认完成原定对话或离场。
<%_ } _%>
<%_ } else { _%>
## 未登场
E25触发前不呈现猪头屠夫与幸福之家厨房。
<%_ } _%>
