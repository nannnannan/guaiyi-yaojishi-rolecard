# 概念·成就·拥抱命运（事件["E334"]）

- 类别：成就
- 事实门槛：对应事件完成或变形且正文实际出现后，才引用其事实；事件中的参考经过不代替玩家选择。

<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E334.状态", { defaults: "未触发" }))) { _%>
## 变体·事件展开
- 门控：E334完成或变形
- 事件演进：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E334.状态", { defaults: "未触发" }))) { _%>
  E334：【拥抱命运】是系统对一次物理拥抱命运规则神显化的特殊结算。 说明文本强调，成就原意是鼓励宿主在心境与精神上接受自己的命运，物理拥抱并非必要； 本次却因实际接触羽毛笔显化而满足低概率触发条件。 E334中羽毛笔允许林恩拥抱一次，系统随即提示道德下降，并发放【拥抱命运】成就、二百点基础经验和永久增益。 ①触发需要对命运产生实际接受行为，物理接触只是本次成立路径。 ②奖励为二百点基础经验、全属性增加五、羽毛笔基础亲密度增加十。 ③使用羽毛笔时受到的反噬减一，作用对象限定为羽毛笔相关使用。 ④系统同时结算道德减一，说明触发过程包含被系统识别的越界倾向。
<%_ } _%>
- 当前状态：
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E334.状态", { defaults: "未触发" }))) { _%>
  限制与代价：本次只结算一次拥抱的现场结果；E334中羽毛笔在一次拥抱后会直接阻止继续越界，成就和亲密度不会替玩家推进更深接触，也不把两人关系结算为恋爱。反噬减一的单位、下限、覆盖的能力范围和是否能抵消规则级反噬均未说明。
<%_ } else { _%>
  限制与代价：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E334.状态", { defaults: "未触发" }))) { _%>
  未知项：非物理路径的具体触发法、重复触发可能、反噬数值算法、全属性增加五的面板落点、道德下降与成就奖励是否必然绑定均未确认。
<%_ } else { _%>
  未知项：进一步范围尚未确认。
<%_ } _%>
<%_ if (["完成","变形"].includes(getvar("stat_data.事件.锚点状态.E334.状态", { defaults: "未触发" }))) { _%>
  禁止外推：物理拥抱不是唯一触发途径，反噬减一未给出可抵消一切反噬的算法。
<%_ } else { _%>
  禁止外推：进一步范围尚未确认。
<%_ } _%>
<%_ } else { _%>
## 变体·兜底
- 门控：兜底（上述事件未完成或变形）
- 事件演进：尚未形成此事物的现场记录。
- 当前状态：名称仅供辨识，来源、能力与归属待实际出现后确认。
<%_ } _%>
